# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Outputs (large, generated)
outputs/
!outputs/.gitkeep
wandb/

# SLURM logs
slurm_logs/

# Probing results
probing_results/

# Checkpoints
*.pt
*.pth
*.ckpt

# Data (downloaded on cluster)
/data/
habitat_data/

# Habitat repos (cloned on cluster, not part of our project)
habitat-sim/
habitat-lab/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
*.log

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.out
*.fls
*.fdb_latexmk
*.synctex.gz

# Reference papers (not redistributable)
docs/EMERGENCE*OF*MAPS*.pdf

# Claude Code session config / worktrees
.claude/

# Backup files
*.bak
*.before_claude_sync.bak

# Local research outputs / audit artifacts (downloaded papers, etc.)
literature/audit/
literature/_research_output.md
literature/*/*.pdf
docs/An active foveated gaze prediction*.pdf
docs/MERGENCE OF MAPS*.pdf

# Large release checkpoints (790 MB — distributed via Hugging Face, not git)
release_ckpts/

# Big experiment outputs (4-8 GB each — kept on cluster, not git)
results/visual_ablation/
results/gps_ablation/
results/excursion_results/
results/tsne_subsamples/

# Trajectory NPZs (~500KB each, but 6+ accumulated; gitignore by default,
# keep summary JSONs which are smaller and human-readable)
results/shortcut_results/*_traj.npz

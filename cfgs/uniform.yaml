# ============================================================
# Uniform-vision agent — baseline
# Same architecture, full-resolution input, no gaze control
# ============================================================

defaults:
  - base

run_name: uniform_agent

foveation:
  enabled: false
  gaze_action: false

# Python deps for this project.
#
# NOTE: For the FULL pipeline (training, rollout, eval on Habitat), you also
# need habitat-sim + habitat-lab + habitat-baselines, which CANNOT be
# installed via pip alone:
#   - habitat-sim 0.3.3 must come from conda (`aihabitat` channel)
#   - habitat-lab + habitat-baselines must be installed from source with a
#     one-line patch (see README.md "Path 2 — Izar setup")
# Use docker/Dockerfile or the README setup paths for the full env.
#
# This requirements.txt covers the pure-Python deps needed for:
#   - analysis-only setups (Path 3 in README, no Habitat sim)
#   - additional pip installs after the conda/habitat install (Path 2)
#
# Pinning rationale: matches docker/Dockerfile so any environment built from
# this list is byte-compatible with the RCP image
# (registry.rcp.epfl.ch/dhlab-wxu/habitat:v2).

# --- Deep learning core ------------------------------------------------------
torch==2.1.0
torchvision==0.16.0
# For CUDA install: pip install torch==2.1.0 torchvision==0.16.0 \
#     --index-url https://download.pytorch.org/whl/cu121
# CPU-only (analysis path): default PyPI index is fine.

# --- RL / environments helpers ----------------------------------------------
gymnasium>=0.29
stable-baselines3>=2.2

# --- Numerical / scientific stack (pinned to match Habitat constraints) ------
numpy<2                       # habitat-sim 0.3.3 incompatible with numpy 2.x
scipy>=1.10
scikit-learn>=1.3
einops>=0.7

# --- Image / video I/O -------------------------------------------------------
pillow==10.4.0                # torchvision 0.16 ABI; newer pillow breaks
opencv-python<4.10            # Habitat scene rendering compatibility
imageio>=2.31
mediapy>=1.2

# --- Habitat downstream (config / utilities) ---------------------------------
omegaconf>=2.3
hydra-core>=1.3
protobuf>=4.21,<5             # torch 2.1 bundled libprotobuf — wider range segfaults

# --- Probing analyses --------------------------------------------------------
torchmetrics>=1.2
statsmodels>=0.14
ripser>=0.6                   # persistent homology in scripts/probing/*
persim>=0.3

# --- Plotting / figures ------------------------------------------------------
matplotlib>=3.7
seaborn>=0.12
plotly>=5.18                  # interactive t-SNE viewers

# --- Notebooks & jobs --------------------------------------------------------
ipykernel>=6.27
jupyter
tqdm>=4.66
loguru>=0.7
wandb>=0.16                   # optional; only needed if training logs to W&B

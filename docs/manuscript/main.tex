\documentclass{article}

% Double-blind main-track submission.
\usepackage[preprint]{manuscript}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{multirow}
\usepackage{placeins}  % \FloatBarrier to keep figures with their text

% Loosen LaTeX float placement: allow figures to take more of a page
% and require less text per page so figures don't all defer to the end.
\renewcommand{\topfraction}{0.92}
\renewcommand{\bottomfraction}{0.92}
\renewcommand{\textfraction}{0.05}
\renewcommand{\floatpagefraction}{0.7}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{4}

% Visible placeholder macro for numbers pending experiment completion.
% Final submission: audit \TODO and replace each with real values.
\newcommand{\TODO}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}

% Uncertainty-flagging macros (final submission: review whether each \uncertain
% should remain coloured or be promoted to settled black).
% \uncertain{} marks an inline claim as interpretive / not directly tested:
%   the prose still reads naturally, the colour signals "this is reading
%   beyond what the data directly shows".
% \pendnote{} converts a "single-seed / multi-seed left for future work / TODO" caveat
%   into a footnote so the main paragraph reads cleanly.
% \uncertain inlines a hedge in plain black; \pendnote becomes a plain footnote
% (no colour) — submission style choice 2026-05-06.
\newcommand{\uncertain}[1]{#1}
\newcommand{\pendnote}[1]{\footnote{\small #1}}

% \hcpending{} flags claims that depend on incoming high-compute (hc)
% cluster results currently in training; the prose reads naturally and
% the colour signals "this is the predicted/expected reading; concrete
% numbers will replace the placeholder once the result lands". Distinct
% colour from \uncertain (orange) so reviewers can tell apart "interpretive
% beyond data" from "awaiting data".
\definecolor{hcpendingcol}{RGB}{0,128,128}
\newcommand{\hcpending}[1]{{\color{hcpendingcol}#1}}

% \todoblue{} — INTERNAL marker (not for reviewers). Flags analyses /
% experiments that are short-compute (≤1 day analysis-only OR overnight)
% and therefore NOT genuinely "future work" — they are this paper's
% pre-DDL priority queue. Workflow: pick one, run it, replace the
% \todoblue{} block with results in plain black. Anything still flagged
% blue at submission time gets either (a) downgraded to "future work" if
% truly out of time, or (b) inlined-as-result if landed. Distinct colour
% from \uncertain (orange) and \hcpending (teal) so we can grep just the
% blue queue. NOTE: strip these (or convert to plain text) before camera-
% ready / reviewer-facing build.
\definecolor{todobluecol}{RGB}{30,80,200}
\newcommand{\todoblue}[1]{{\color{todobluecol}#1}}

% \toconfirm{} — INTERNAL marker (not for reviewers). Flags specific
% quantitative results (R², SPL, participation ratios, ID estimates,
% cluster centroids, etc.) that come from the OLD pre-unified-retrain
% checkpoints and therefore must be re-computed once the unified-retrain
% probes (probe-1...4) finish converging. Workflow: post-retrain we
% re-probe all 5 ckpts, then sweep \toconfirm{...} sites and replace
% with the converged number in plain black. Any number still flagged
% red at submission time is a known-stale value that we either failed
% to update (panic) or explicitly chose to keep (rare). Pairs with
% \todoblue{} (which queues NEW analyses): \toconfirm tracks UPDATING
% existing analyses; \todoblue tracks NEW ones. Distinct colour
% (red) from \uncertain (orange), \hcpending (teal), \todoblue (blue).
% NOTE: strip / replace before camera-ready.
\definecolor{toconfirmcol}{RGB}{200,30,30}
\newcommand{\toconfirm}[1]{{\color{toconfirmcol}#1}}

\title{Sensor structure shapes the format of cognitive maps in navigation agents}

\author{%
  Anonymous authors\\
  Paper under double-blind review\\
}

\begin{document}
\maketitle

\begin{abstract}
The format of spatial memory in the hippocampus varies systematically with a species' visual ecology, but whether this reflects a general sensor--memory coupling is hard to test directly: animal sensor ecology is fixed within species, and AI systems rarely co-train encoder and memory along a sensor axis. We test this in silicon: a navigation agent where only the visual sensor varies along an axis of encoder spatial bandwidth, with architecture, task, and training held fixed. Across five conditions, agents reach near-identical task performance yet the hidden-state code for position differs in form. With a vision-limited encoder, position concentrates into a scene-invariant direction linearly readable from the hidden state; with a rich encoder, it distributes into non-linear directions and partly offloads to the encoder. Linear readability shifts faster than total position information --- a format shift, not a total-information collapse. We frame this as an emergent \emph{capacity allocation} between the encoder route and the memory-integration route, measured along three orthogonal axes (magnitude, format, consumption). Memory transplants are asymmetric (rich-encoder policies fail on bottleneck states; the reverse works), paralleling cross-modality remapping in hippocampal coding under hidden-state inference. Probe-readable position further dissociates from policy reliance: a bottleneck-encoder agent under-weights its readable code while a rich-encoder agent reads a non-spatial substitute. This positions sensor structure as a training-time lever on cognitive-map format, generates a falsifiable architecture-agnostic prediction for transformer navigators, and reframes bandwidth-restricting perception as potentially cognition-enabling rather than only compute-saving.
\end{abstract}


\section{Introduction}

Visual perception and downstream memory in artificial systems are commonly treated as a pipeline: scale the encoder, and any downstream module benefits. Recent benchmarks complicate this picture --- vision--language models with state-of-the-art surface visual recognition still fail at fine-grained spatial reasoning~\citep{ramakrishnan2025space,yang2024thinkinginspace}, with two competing diagnoses: a training-distribution gap~\citep{chen2024spatialvlm}, and an encoder-side argument that pretrained encoders carry markedly different geometric content depending on training objective~\citep{tong2024eyeswideshut,elbanani2024probing3d}. We pursue an even sharper structural reading: the encoder and the memory downstream of it are a co-trained system, and \emph{the encoder's structure shapes what is linearly readable from the memory about space} --- a content-level claim about the perception--memory interface.

Cognitive neuroscience offers concrete precedent for this at the species level: primate view-cells under foveated saccades versus rodent place-cells under near-panoramic vision~\citep{wirth2017gaze,rolls2024why}; cross-modality remapping in echolocating bats whose hippocampal coding reorganises between vision and echolocation~\citep{geva2016remapping}; congenitally blind humans showing larger anterior hippocampal volume and supranormal non-visual navigation~\citep{fortin2008blind}. Whether this generalises to artificial systems is hard to test in animals --- their sensor ecology is fixed within species, and modern AI systems rarely co-train encoder and memory from scratch along a sensor axis. We test it in silicon, where this design degree of freedom is available.

\noindent Beyond the specific bandwidth--allocation hypothesis, the matched-condition design we adopt opens a methodological avenue that biological neuroscience cannot easily realise. Comparative hippocampal studies (rats, bats, monkeys, blind humans) cannot hold sensor structure independent of evolutionary lineage, body plan, or learning history; cross-species effects are confounded with everything else that differs across species. In a silicon analogue this confound dissolves: encoder, recurrent module, action repertoire, training task, optimiser, and reward structure are identical, and only the sensor varies. This invites the systems-neuroscience analytic toolkit --- population probes, intrinsic timescales, dynamical-systems portraits, mixed-selectivity / splitter analyses, predictive-coding residuals, persistent-homology of the state manifold --- to be applied to a \emph{controlled comparative population}, not to a single recording. Our paper treats this comparative methodology as an explicit secondary contribution: alongside the empirical capacity-allocation result, we report a set of cogneuro-style analyses on the five-condition population, framing the work as a step toward a comparative cognitive neuroscience of artificial navigation agents.

\begin{figure}[t]
\centering
% Paper-level pipeline schematic. Row 1 (architecture): condition-thumbnail
% column -> ResNet-18 -> sensor stack -> 3-layer LSTM -> policy / h_2.
% Row 2 (analysis): three method-finding columns (probes, geometry,
% intervention) read the frozen top-layer hidden state h_2.
\setlength{\abovecaptionskip}{4pt}
\includegraphics[width=\linewidth]{fig/fig_pipeline_overview_tikz.pdf}
\caption{\textbf{Experimental setup and analysis pipeline.} \emph{Row~1 (architecture):} four sensor conditions span an axis of encoder spatial bandwidth (\emph{blind}, \emph{coarse}, \emph{foveated}, \emph{uniform}; leftmost column), feeding a shared ResNet-18 encoder $+$ 3-layer LSTM; \emph{blind} bypasses the encoder. A log-polar foveation control (Appendix~\ref{app:scaling}) adds a 5th intermediate point on the same axis. \emph{Row~2 (analysis):} three method blocks --- decodability, intrinsic structure, causal intervention --- read the frozen top-layer hidden state $\mathbf{h}_2 \in \mathbb{R}^{512}$ to test three axes of one capacity allocation: \emph{magnitude}, \emph{format}, \emph{consumption} (§\ref{sec:methods-setup}; synthesis in §\ref{sec:discussion}).}
\label{fig:setup}
\end{figure}

Cognitive-map-like representations emerge in the recurrent state of deep-RL navigation agents even with no vision at all~\citep{wijmans2023emergence,banino2018grid,cueva2018grid},\footnote{\small ``Cognitive map'' in our paper is operationalised as the linearly-decodable world-frame position code at the policy-readable hidden state. Aspects of the broader concept~\citep{epstein2017cognitive} are engaged by our LOSO scene-invariance test (relational across rooms) and predictive-horizon analysis (SR-style future-state code, \citealt{stachenfeld2017predictive}); hierarchical scene clustering and topological reachability are deferred to follow-up.} but the literature studies one condition at a time, leaving open how encoder structure interacts with what the memory builds. We adopt this agent family as a comparative chassis (Figure~\ref{fig:setup}): identical encoder--memory--policy pipeline, task, and training; varying only the visual sensor across five conditions on an axis of encoder spatial bandwidth (§\ref{sec:methods-setup}).

\emph{If} the visual encoder and the recurrent integration of GPS-sensor history are competing routes for a finite recurrent-memory budget, \emph{then} richer encoders should erode the linear-readable position signal in the recurrent state while leaving task performance intact. We observe exactly this: across the five sensor conditions, top-layer linear GPS $R^2$ falls monotonically with encoder spatial bandwidth while success rate stays at $93$--$99\%$. We frame this as an emergent \emph{capacity allocation} between visual and integration routes and test it along three orthogonal axes: \emph{magnitude} (how much linear position information lives in the recurrent state), \emph{format} (which subspace carries it across conditions), and \emph{consumption} (which route the policy actually uses for behaviour). Cross-method convergence across linear/MLP/MINE probes, leave-one-scene-out validation, Procrustes shape distance, $5{\times}5$ memory transplant, and shortcut-discovery tests rules out single-method artefacts; the asymmetric direction-of-transplant result establishes the format claim outside any probe framework~\citep{orozco2025emergentopenvla}.

\noindent\textbf{Contributions.} (i) A 5-condition controlled silicon model isolating sensor-induced spatial-format change while holding encoder backbone, task, training, and reward fixed. (ii) The \emph{capacity-allocation} principle: a single mechanism organising five concomitant signatures (magnitude collapse, format divergence, consumption dissociation, scene-invariance, place-vs-view-cell character) into three orthogonal axes. (iii) An asymmetric memory-transplant test (rich-encoder policies fail to use bottleneck states; bottleneck policies still drive themselves from rich-encoder ones) that mirrors hippocampal cross-modality remapping at the behavioural level. (iv) A falsifiable architecture-agnostic prediction for transformer-based navigators (§\ref{sec:discussion}, cf.~\citealt{whittington2022transformerhippocampus}). (v) A controlled testbed for porting the systems-neuroscience analytic toolkit (population geometry, intrinsic timescales, fixed-point dynamics, predictive-coding residuals, splitter-cell mixed selectivity, persistent homology of the state manifold) to artificial navigation agents, providing the controlled counterpart to cross-species hippocampal comparisons that biological neuroscience cannot easily achieve. For visual intelligence, capacity allocation offers an architecture-side complement to data-side accounts~\citep{chen2024spatialvlm} of VLM spatial-reasoning failures, and reframes bandwidth-restricting perception (foveation, log-polar) as potentially cognition-\emph{enabling} rather than only compute-saving.

\section{Methods}
\label{sec:methods-setup}

\noindent\textbf{Architecture, task, and conditions (Figure~\ref{fig:setup}, Row 1).}\quad PointGoal navigation in Habitat~\citep{savva2019habitat} trained with DD-PPO~\citep{wijmans2020ddppo} across five conditions varying only the visual sensor: \emph{blind} (no encoder); \emph{coarse} ($48{\times}48$ RGB, $1{\times}1$ encoder feature map after ResNet-18 downsampling); \emph{foveated} ($256{\times}256$ with eccentricity-dependent Gaussian blur $\sigma_{\max}{=}8$, $4{\times}4$ feature map); \emph{uniform} ($256{\times}256$ unblurred, $4{\times}4$); \emph{foveated-logpolar} ($\sim 2{\times}2$ via log-polar resampling onto a $64{\times}64$ retinal grid, control point isolating encoder-spatial-output from blur, Appendix~\ref{app:scaling}). All five share an identical 3-layer LSTM (512-d) and the Wijmans non-visual sensor stack (goal-in-start-frame, GPS, compass, close-to-goal); Gibson-0+~\citep{xia2018gibson} and MP3D training pool; 18 MP3D test scenes for distribution-shift probes; 250M frames each; single seed per condition (cogsci modelling convention~\citep{wijmans2023emergence,banino2018grid,cueva2018grid,schaeffer2023grid}; rigour from convergent multi-method evidence, §\ref{sec:limitations}); blind has an independently retrained checkpoint (Appendix~\ref{app:repro}). After training, weights are frozen and per-step top-layer hidden state $\mathbf{h}_2 \in \mathbb{R}^{512}$ is collected from $n=500$ Gibson held-out deterministic-rollout episodes per condition.

\noindent\textbf{Probing methodology (Figure~\ref{fig:setup}, Row 2).}\quad Three method blocks read $\mathbf{h}_2$ to test three orthogonal axes of one capacity allocation:
\begin{itemize}\setlength{\itemsep}{2pt}\setlength{\topsep}{2pt}
  \item \emph{Decodability} $\to$ \emph{magnitude} axis: linear Ridge with Hewitt--Liang selectivity~\citep{hewitt2019control}, 2-layer MLP, MINE~\citep{belghazi2018mine}, Skaggs spatial information~\citep{skaggs1996theta}; plus step-binned, lag-$k$, predictive-horizon~\citep{stachenfeld2017predictive,whittington2020tem}, and excursion-forgetting variants for memory dynamics.
  \item \emph{Intrinsic structure} $\to$ \emph{format} axis: LOSO cross-validation~\citep{sanders2020remapping}, cross-condition probe transfer, CKA~\citep{kornblith2019cka}, 1-NN purity, Procrustes shape distance~\citep{williams2021shape}, principal angles and position-direction cosines, participation ratio and TwoNN intrinsic dimension~\citep{ansuini2019intrinsic,facco2017twonn}, eigenspectrum power-law~\citep{stringer2019high}.
  \item \emph{Causal intervention} $\to$ \emph{consumption} axis: $5{\times}5$ memory transplant and memory-init transplant; paired same-scene shortcut-discovery (Tolman 1948 in silico); GPS-sensor mid-rollout ablation.
\end{itemize}
Each method is introduced at point of use in §\ref{sec:results}; full protocols and hyperparameters in Appendix~\ref{app:probing-details}. Convergence across the three axes on the same condition-level contrast is the strongest claim the methodology supports.


\section{Results}
\label{sec:results}

\noindent The five agents reach near-identical task performance (Table~\ref{tab:summary}) despite substantially different visual inputs --- the encoder--memory--policy stacks must therefore internally compensate for the input differences, holding different internal representations of the same task. We read those representations along five axes: how much linearly-readable position lives in memory (magnitude, §\ref{sec:h1}); what shape it takes spatially (format--spatial, §\ref{sec:format-spatial}) and temporally (format--temporal, §\ref{sec:tgm}); how the policy uses it (consumption, §\ref{sec:dissociation}); and how behaviour responds when we intervene (causal intervention, §\ref{sec:causal-intervention}). Each axis points to the same underlying capacity-allocation tradeoff between an encoder-derived visual route and a memory-integration route. The Discussion (§\ref{sec:discussion}) applies the same set of analyses to a different environment and encoder family, as a step toward a comparative cognitive neuroscience of artificial navigation agents.

\subsection{Magnitude axis: linear position code shrinks with encoder bandwidth}
\label{sec:h1}

\noindent\textbf{Headline.}\quad As encoder spatial bandwidth grows, the linearly-readable world-frame position signal at the policy-readable LSTM layer shrinks monotonically (Figure~\ref{fig:magnitude}a). All five agents reach near-ceiling task performance (Table~\ref{tab:summary}; a Bug-style controller without learning is an order of magnitude below them), so this is a representational dissociation rather than a skill gap. The position information itself is not lost --- a non-linear MLP probe still recovers it (Figure~\ref{fig:magnitude}c, §\ref{sec:format-spatial}); what shrinks is the linear, policy-accessible part.

\begin{table}[!t]
\centering
\small
\setlength{\tabcolsep}{6pt}
\caption{Per-condition headline. All five agents trained for 250M frames (§\ref{sec:methods-setup}); blind uses an independently retrained checkpoint (Appendix~\ref{app:repro}). Probe $R^2$: 5-fold episode-level CV mean $\pm$ std on full-length trajectories (no per-episode step cap). Sub-chance $R^2$ in rich-encoder rows reflect long-tail collapse (mid-episode $R^2 \in [0.6, 0.95]$, Figure~\ref{fig:h1_mega}b); rich-encoder GPS $R^2$ is statistically indistinguishable from zero (one-sample $t$-test: foveated $p\approx 0.58$, uniform $p\approx 0.25$; $df{=}4$). Bold marks blind/coarse rows where GPS and compass remain decodable ($p<0.001$) and best task performance.}
\label{tab:summary}
\begin{tabular}{lcccc}
\toprule
Condition & SPL & Success & GPS $R^2$ (no-cap) & Compass $R^2$ (no-cap) \\
\midrule
Blind                 & 0.59          & 0.93          & \textbf{$+0.934\pm0.04$} & \textbf{$+0.844\pm0.03$} \\
Coarse                & 0.853         & 0.989         & \textbf{$+$0.582$\pm$0.099} & \textbf{$+$0.659$\pm$0.061} \\
Foveated              & \textbf{0.894}& \textbf{0.993}& $+$0.178$\pm$0.659 & $+$0.503$\pm$0.142 \\
Foveated-logpolar     & 0.879         & 0.990         & $-$0.029$\pm$0.759 & $+$0.467$\pm$0.330 \\
Uniform               & 0.891         & \textbf{0.993}& $-$1.785$\pm$2.993 & $-$1.244$\pm$3.579 \\
\bottomrule
\end{tabular}
\end{table}

\noindent\textbf{What the contrast is, and what it is not.}\quad The contrast is specifically about whether a linear top-layer readout --- the operation the DD-PPO policy head performs --- can recover position from $\mathbf{h}_2$. Two confounds are addressed up front: goal-distance decodes comparably across conditions (which argues against a generic probe-capacity reading), and the foveated-logpolar control at $\sim 2{\times}2$ output sits with the rich-encoder cluster (which is hard to reconcile with pure spatial-output dimensionality and motivates a spatial-feature-\emph{variety} reframing, Appendix~\ref{app:scaling}). The blind result replicates~\citep{wijmans2023emergence}; coarse extends it to a vision-with-bottleneck condition, which suggests the shared trigger is minimal per-step feature variety rather than absence of vision per se.

\noindent\textbf{Mechanism: capacity allocation between two routes.}\quad We read the magnitude collapse as a \emph{capacity-allocation} pattern. The hidden state has two routes to a position code: integrating the L0 GPS-sensor input across time, or reading position-correlated features from the encoder. Policy gradient biases capacity toward whichever route the encoder makes viable; bottleneck encoders leave only the integration route, while rich encoders supply a viable visual route and the integrated code becomes redundant for the policy. Three converging views are consistent with this reading. \emph{Across training} (Figure~\ref{fig:magnitude}b): all sighted conditions start with high linear readability at $\sim$50M frames; the rich-encoder conditions then decay along trajectories whose timescale tracks encoder informativeness, while coarse plateaus and blind remains stable. \emph{Pipeline localisation} (Figure~\ref{fig:magnitude}c): linear readability at the LSTM \emph{input} is comparable across conditions and only diverges at L2 (the policy-readable layer); at L2, the linear$\,\to\,$MLP gap is large for the rich-encoder conditions, consistent with their L2 collapse being a \emph{format} change rather than an information loss. \emph{Within-episode} (step-binned, Appendix Figure~\ref{fig:h1_mega}): rich-encoder readability is preserved mid-episode and weakens in the long tail, consistent with the integrated code drifting when visual input dominates the cue.

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/fig_magnitude.pdf}
\caption{\textbf{Capacity allocation along the magnitude axis.} \emph{(a)} Linear GPS $R^2$ at $\mathbf{h}_2$ falls monotonically with encoder bandwidth (5-fold CV; foveated-logpolar at $\sim 2{\times}2$ lands in the rich regime). \emph{(b)} The collapse develops over training only for rich-encoder conditions; bottleneck conditions hold the linear code throughout. \emph{(c)} The cross-condition divergence sits at L2 (policy-readable layer), not at the encoder or LSTM input; star markers show that an MLP probe still reads position at L2 --- the linear$\,\to\,$MLP gap is the format shift quantified in §\ref{sec:format-spatial}. Post-retrain hp-consistent agents (Appendix~\ref{app:repro}); panel-level detail in Appendix Figures~\ref{fig:h1_mega}, \ref{fig:substitution_dynamics}, \ref{fig:predictive_horizon}.}
\label{fig:magnitude}
\end{figure}

% --- WJ-A memory-length sweep stub commented out 2026-04-27 09:15 ---
% Eval-time periodic-reset protocol confounded by per-step GPS-sensor
% leakage into L0 input. Results in SLEEP_LOG.md; experiment not paper-
% ready in current form. Possible reframe (architectural memory restriction
% during training) is out of scope for this submission.
% \noindent\textbf{Memory-length budget sweep}\quad \hcpending{...}

\noindent\textbf{Population-level corroboration.}\quad Two cogneuro signatures are consistent with the capacity-allocation reading. \emph{(1) Peaked-unit content.} Per-unit Skaggs spatial information~\citep{skaggs1996theta} is condition-flat, but peaked-unit regression (Appendix Figure~\ref{fig:place_cells}) suggests that the most informative units in rich-encoder agents track trajectory-phase / visual-landmark features more strongly than raw position, while in blind they track raw position more strongly --- a view-cell-like vs.\ path-integrated dissociation~\citep{rolls2024why}. \emph{(2) Robustness profile.} A Stachenfeld-style predictive-horizon probe~\citep{stachenfeld2017predictive} (Appendix Figure~\ref{fig:predictive_horizon}) and a 25-step random-action mid-episode excursion test (Appendix Figure~\ref{fig:wjf_excursion}) converge: blind sustains long predictive horizons over future positions (an SR-style cognitive-map signature) and fragments most under excursion perturbation. The pattern is the inverse of a passthrough hidden state: the more a condition's $\mathbf{h}_t$ integrates the action stream, the more it fragments when that stream is perturbed --- consistent at the high level with rodent path-integration under removed visual landmarks~\citep{chen2016grid}.

\subsection{Format axis (spatial): scene-invariance within and non-interchangeable subspaces across}
\label{sec:format-spatial}
\label{sec:h3-scene-invariance}
\label{sec:h2}

\noindent\textbf{Linear collapse is a format shift, not a total-information loss (Figure~\ref{fig:format_shift}).}\quad The magnitude-axis collapse is sharpened by three converging measurements: linear (Ridge) and 2-layer MLP probes on the same $\mathbf{h}_2$ show that the linear--MLP gap widens with encoder bandwidth (rich-encoder conditions where linear is at chance still admit non-linear recovery), and MINE estimates of total $I(\mathbf{h}_2; \text{pos})$ shift far less than the linear $R^2$ (Appendix~\ref{app:mine}). Read together, position information appears to be approximately preserved by non-linear readout while linear readability collapses --- a format shift rather than an information loss. Since the DD-PPO action head is a linear projection of $\mathbf{h}_2$, ``linearly decodable at L2'' maps directly to ``policy-accessible''. The remaining question is what changes \emph{format} between bottleneck and rich-encoder conditions; two views, within and across conditions, agree.

\begin{figure}[!t]
\centering
\includegraphics[width=0.6\linewidth]{fig/fig_information_allocation.pdf}
\caption{\textbf{Format shift, not info collapse.} Linear (filled, dotted) and 2-layer MLP (hollow, solid) probes per condition; same episode-level 5-fold CV, $20\,000$ episode-step samples per condition. Linear $R^2$ range $\approx 2.0$ across conditions; MLP range $\approx 0.5$. Position information is approximately preserved by MLP probing in rich-encoder conditions even when linear readability collapses; MINE-estimated total $I(\mathbf{h}_2; \text{pos})$ decreases by only $\approx 1.3\times$ across the same range (Appendix~\ref{app:mine}). The remaining variation is in \emph{format}, the focus of the rest of this section.}
\label{fig:format_shift}
\end{figure}

A leave-one-scene-out (LOSO) cross-validation localises the within-condition answer (Figure~\ref{fig:loso_cv}): blind achieves a high median LOSO $R^2$ with most held-out scenes positive, while rich-encoder conditions drop to chance-level medians with a substantial fraction of held-out scenes giving negative $R^2$, and the bottleneck-vs-rich-encoder rank ordering is preserved across the cross-training trajectory (Figure~\ref{fig:magnitude}b). We read this as: encoder bandwidth modulates not the availability of position information in $\mathbf{h}_2$ (preserved per the MLP probe) but the \emph{scene-invariance} of the linear readout direction. A bottlenecked encoder lacks scene-specific features, so a single linear hyperplane suffices for cross-scene readout; a rich encoder lets the LSTM use scene-specific visual features, inducing scene-conditional position-directions that an MLP can switch over but a single linear probe cannot. This re-states capacity allocation in IB-invariance terms~\citep{achille2018invariance}; we read the dichotomy as analogous to the no-remapping vs.\ global-remapping spectrum of hippocampal coding under hidden-state inference~\citep{sanders2020remapping}, with limited per-step visual evidence supporting a single hidden-state context (no remap) and rich evidence supporting distinct scene-contexts (global remap).

\begin{figure}[!t]
\centering
\includegraphics[width=0.6\linewidth]{fig/fig_loso_cv.pdf}
\caption{\textbf{Leave-one-scene-out (LOSO) cross-validation: encoder bandwidth dictates scene-invariance of the linear position readout.} For each condition, Ridge probe trained on $\sim 30$ Gibson scenes and tested on the held-out scene. Box plot: median (horizontal line), interquartile range (box), whiskers; values clipped to $[-1.5, 1]$ for visualisation. Blind: $94\%$ of held-out scenes give $R^2 > 0$; rich-encoder conditions: $38$--$48\%$ of scenes give negative $R^2$. The bottlenecked encoder forces a scene-invariant position-direction (linearly probable across scenes); a rich encoder lets the LSTM use scene-specific visual features, producing scene-conditional encoding that requires a non-linear readout to switch contexts.}
\label{fig:loso_cv}
\end{figure}

If conditions allocate hidden-state capacity to different subspaces of $\mathbf{h}_2$, the resulting representations should be hard to interchange: a probe trained on one condition should not read another, and a memory transplant should fail. The $5{\times}5$ transplant matrix at midpoint $t{=}30$ (Figure~\ref{fig:h2}, right) shows a direction-asymmetry: when a bottleneck condition (blind) provides the donor and a rich-encoder condition is the recipient, recipient SPL drops markedly; reversed (rich-encoder donor, bottleneck recipient), recipient SPL is approximately preserved. \emph{Rich-encoder policies have difficulty using bottleneck-style hidden states; bottleneck policies still drive themselves from rich-encoder ones} --- a directional asymmetry analogous to cross-modality remapping in echolocating bats, where the same hippocampal neurons recode under vision vs.\ echolocation~\citep{geva2016remapping}. Recipient sensitivity scales with encoder bandwidth (uniform most brittle, blind least), and condition pairs that look similar under linear CKA still differ under transplant. Cross-condition probe transfer (Figure~\ref{fig:h2}, left) mirrors the asymmetry: blind$\to$rich-encoder transfer degrades mildly while rich-encoder$\to$blind degrades severely. 1-NN purity is at ceiling on pooled top-layer states; probe-overfit accounts are mitigated by 5-fold episode-level CV and Hewitt--Liang selectivity tracking each probe's $R^2$.

\begin{figure}[!t]
\centering
\begin{minipage}[t]{0.49\linewidth}\centering
\includegraphics[width=\linewidth]{fig/fig4_h2_probe_transfer.pdf}\\[-2pt]
{\footnotesize\textbf{(a) Cross-condition probe transfer}}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\linewidth}\centering
\includegraphics[width=\linewidth]{fig/fig4_transplant_5x5.pdf}\\[-2pt]
{\footnotesize\textbf{(b) $5{\times}5$ memory-transplant matrix}}
\end{minipage}
\caption{\textbf{Format axis (across-condition): non-interchangeable subspaces, two views.} \emph{(a)} Cross-condition probe transfer for GPS $R^2$ (train on row, test on column; signed-log colour scale; diagonal = self-probe). \emph{(b)} $5{\times}5$ cross-condition memory-transplant matrix at midpoint $t{=}30$; off-diagonal cell $=$ cross-transplant SPL $-$ recipient's self-transplant SPL ($6$ coarse cross-pair cells excluded due to differing visual input size). Both views show the same direction-asymmetry: bottleneck $\to$ rich-encoder transplant breaks recipient (mean SPL drop $-0.23$), rich-encoder $\to$ bottleneck preserves it (mean $\Delta \approx 0$). Single seed; midpoint-sweep companion in Appendix~\ref{app:supp_figs}.}
\label{fig:h2}
\end{figure}

Three methodologically distinct geometric views are consistent with this picture (Table~\ref{tab:geometry}). \emph{Procrustes shape distance}~\citep{williams2021shape} (a Riemannian metric on Kendall's shape space) shows a bandwidth-aligned pattern: the bottleneck pair (blind--coarse) and the rich-encoder pair (foveated--uniform) are tighter than cross-regime pairs. \emph{Participation ratio and TwoNN intrinsic dimension}~\citep{ansuini2019intrinsic}: coarse separates from the others, consistent with $1{\times}1$ encoder collapse pulling down representational dimensionality. \emph{Eigenspectrum power-law}~\citep{stringer2019high}: the four sighted conditions cluster around the Stringer optimum, while blind sits well above with a sharp tail decay (variance concentrated in a few dominant top PCs). PR distinguishes coarse's encoder-collapsed regime; $\alpha$ distinguishes blind's tightly-decayed tail. We use Stringer's framing descriptively rather than mechanistically; methodological details are in Appendix~\ref{app:eigenspectrum}.

\begin{table}[!t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\caption{\textbf{Representational geometry.} \emph{Within-condition (left):} PR = participation ratio (effective dimensions); TwoNN ID-MLE and ID-CDF (intrinsic dimension); $\alpha$ = eigenspectrum power-law exponent (PCs $5$--$200$, Stringer 2019; optimum $\alpha = 1+2/d \approx 1.67$). \emph{Cross-condition (right):} Procrustes Riemannian angular distance $\theta_1$ on top-PCA shapes (range $[0, \pi/2]$; larger = more distinct). Bold = column-wise minimum (PR, ID) or closest to optimum ($\alpha$). Coarse separates on PR/ID (encoder collapse); blind on $\alpha$ (sharp tail). $\theta_1$ pattern is bandwidth-aligned: bottleneck pair (blind--coarse) and rich-encoder pair (foveated--uniform) are tighter than cross-regime pairs. $500$ paired Gibson eval episodes; single seed.}
\label{tab:geometry}
\begin{tabular}{lcccccccc}
\toprule
 & \multicolumn{4}{c}{\emph{Within condition}} & \multicolumn{4}{c}{\emph{Procrustes $\theta_1$ to other condition}} \\
\cmidrule(lr){2-5} \cmidrule(lr){6-9}
Condition & PR & ID-MLE & ID-CDF & $\alpha$ & Bl & Co & Fv & Un \\
\midrule
Blind                          & $4.10$ & $1.99$ & $1.85$ & $\mathbf{3.85}$ & $-$    & $1.10$ & $1.23$ & $1.22$ \\
Coarse                         & $\mathbf{3.10}$ & $\mathbf{1.76}$ & $\mathbf{1.64}$ & $1.80$ & $1.10$ & $-$    & $1.14$ & $1.13$ \\
Foveated                       & $\mathbf{4.34}$ & $1.89$ & $1.77$ & $\mathbf{1.72}$ & $1.23$ & $1.14$ & $-$    & $1.08$ \\
Uniform                        & $4.01$ & $1.91$ & $1.80$ & $1.90$ & $1.22$ & $1.13$ & $1.08$ & $-$    \\
\bottomrule
\end{tabular}
\end{table}

If capacity allocation produces near-orthogonal subspaces in $\mathbf{h}_2$ with position encoded along non-aligned directions, principal-angle and direction-cosine tests should both register the divergence (Figure~\ref{fig:subspace_divergence}). Across all ten condition pairs, mean principal angles between top-$K$ PCA subspaces are close to orthogonal, and pairwise cosines between Ridge-probe GPS-direction weights are near zero (within the random-pair null). The divergence appears \emph{early}: at the earliest probed checkpoint ($\sim 50$M frames), principal angles are already near orthogonal and direction cosines near zero (Figure~\ref{fig:subspace_evolution}, Appendix), suggesting the structural separation is established well before convergence rather than emerging late.

\begin{figure}[!t]
\centering
\includegraphics[width=0.92\linewidth]{fig/fig_subspace_divergence.pdf}
\caption{\textbf{Subspace divergence: conditions occupy mostly-orthogonal subspaces of $\mathbf{h}_2$.} \emph{(a)}~Mean principal angles between top-$K$ PCA subspaces ($K \in [4, 33]$ per condition, $\sim 90\%$ variance; $20\,000$ episode-step samples per condition). Off-diagonal angles in $[69°, 87°]$ (most pairs in $[77°, 87°]$); the closest pair (foveated--foveated-logpolar, $69°$) is the same-family rich-encoder pair. \emph{(b)}~Pairwise cosines between position-encoding directions (Ridge regression weight on GPS). All off-diagonal cosines in $[-0.094, +0.102]$ --- position is encoded along essentially uncorrelated directions across conditions. Single seed.}
\label{fig:subspace_divergence}
\end{figure}

\subsection{Format axis (temporal): the blind code rotates over time, sighted codes stabilise}
\label{sec:tgm}

\noindent The format-axis evidence so far compares static snapshots --- LOSO generalisation, Procrustes shape, principal-angle subspace tests. To ask whether the \emph{temporal architecture} of the code differs across conditions, we run a King--Dehaene Temporal Generalisation Matrix~\citep{king2014temporal}: train a ridge regressor of egocentric goal-vector at trajectory step $t_{\text{train}}$ on a held-out episode set, evaluate at step $t_{\text{test}}$, report $R^2$ for every $(t_{\text{train}}, t_{\text{test}})$ pair ($T{=}50$ steps, $n_{\text{eps}}{=}300$ per condition, PCA top-30 pre-reduction, ridge $\alpha{=}10$; protocol pre-registered in \texttt{scripts/probing/extra/temporal\_generalisation.py}). The five $50{\times}50$ matrices (Figure~\ref{fig:tgm}) reveal three distinct temporal architectures: a \emph{diagonal-only} pattern in the \textsc{blind} agent (peak $R^2$ at $t_{\text{train}}{=}t_{\text{test}}$, off-diagonal mean $-0.52$; lag-30 $R^2 = -0.91$, signalling that the linear basis at step $t$ is \emph{incompatible} with the basis at $t \pm 30$), a \emph{square-block} pattern in the \textsc{coarse} 1$\times$1 agent (off-diagonal mean $+0.15$; lag-30 $R^2 = +0.07$ --- the same linear projection still works thirty steps later), and graded intermediate patterns in the three rich-encoder conditions (\textsc{foveated} lag-30 $-0.08$, \textsc{uniform} $+0.03$, \textsc{foveated-logpolar} $-0.15$). \emph{Code half-life} (lag at which off-diagonal $R^2$ crosses zero) ordering: \textsc{coarse} $>$ \textsc{uniform} $>$ \textsc{foveated-logpolar} $>$ \textsc{foveated} $>$ \textsc{blind}. The mechanism this picture supports is that \textsc{blind} must \emph{integrate} motor signals from $t{=}0$ to compute its goal-vector, so the meaning of every $\mathbf{h}_t$ coordinate shifts step-by-step (a recurrent basis rotation analogous to King \& Dehaene's diagonal-MEG signature for transient cortical codes); \textsc{coarse}, by contrast, derives goal-direction from a slowly-varying colour cue at every step, so the $\mathbf{h}{\to}\text{goal-vec}$ mapping is quasi-stationary (their square-block sustained-code signature). The TGM thus provides a temporal-format dissociation that the static probes did not: \textsc{coarse} agents \emph{read} the same goal direction across most of the trajectory; \textsc{blind} agents \emph{recompute} it from the action sequence each step. \uncertain{The pre-registered prediction (filed in \texttt{docs/manuscript/sample/cogneuro\_frameworks/tgm\_king\_dehaene.md}) was the opposite (\textsc{blind} as working-memory \emph{block}, \textsc{coarse} as recompute-each-step diagonal); we report what we found.}

\begin{figure}[!t]
  \centering
  \includegraphics[width=\linewidth]{fig/cogneuro/fig_tgm.pdf}
  \caption{\textbf{Temporal Generalisation Matrix shows blind agent uses a transient code, coarse agent a sustained code.} Each panel: $50{\times}50$ matrix where cell $(i, j)$ is the $R^2$ of an egocentric-goal-vector ridge decoder fit at step $i$ and evaluated at step $j$, 5-fold cross-validated across 300 episodes, PCA top-30 pre-reduction, ridge $\alpha{=}10$. Diagonal-only structure (\textsc{blind}) means the linear read-out for goal direction rotates over time; square-block structure (\textsc{coarse}) means the same linear projection works throughout the trajectory. Sighted-rich conditions interpolate. Method: King \& Dehaene 2014~\citep{king2014temporal}; protocol pre-registered.}
  \label{fig:tgm}
\end{figure}

Intrinsic timescales are consistent with this picture (Figure~\ref{fig:cogneuro_battery}a). If \textsc{blind}'s code is integrating, individual units should retain their values longer per step than in sighted conditions --- the Murray et al.~2014 cortical-hierarchy pattern, where intrinsic autocorrelation $\tau$ grows from sensory cortex through prefrontal cortex~\citep{murray2014timescales}. Fitting per-unit autocorrelation decay (PCA-30, fit $R^2 > 0.5$ kept), \textsc{blind} integrates over a substantially longer timescale than every sighted condition --- a Murray-style fast-sensor / slow-integrator dichotomy on the encoder-bandwidth axis. The slower-$\tau$ \textsc{blind} units co-occur with a rotating linear projection (TGM diagonal): a slow-rotating-spiral picture rather than a stationary attractor. Sighted-rich units change fast yet the projection is stationary (TGM block in \textsc{coarse}, mixed in others) --- a fast-pendulum-with-fixed-axis picture.

A parameter-free topology probe (Figure~\ref{fig:cogneuro_battery}b,c) gives a third view, using the same Vietoris--Rips persistent-homology pipeline Gardner et al.~2022 used for toroidal grid-cell structure in mouse MEC~\citep{gardner2022toroidal}. Across-condition Wasserstein-2 distance between persistence diagrams is roughly twice the within-condition seed-to-seed noise, so conditions are topologically distinguishable. Persistent Betti-1 counts (loops with persistence $>0.1$) order monotonically with encoder bandwidth, with \textsc{blind} highest and \textsc{uniform} lowest. \uncertain{Our pre-registered prediction was the opposite direction; we report what we found.} A reading consistent with the broader picture: \textsc{blind}'s hidden state revisits the same neighbourhood along different routes (path-integration with state aliasing under sensory degeneracy), generating short-lived loops; \textsc{uniform}'s rich visual signal tags locations uniquely, producing a more tree-like manifold with fewer persistent cycles. We treat this topological reading as suggestive corroboration rather than independent mechanistic evidence.

\begin{figure}[!t]
\centering
\begin{minipage}[t]{0.32\linewidth}
\centering
\includegraphics[width=\linewidth]{fig/cogneuro/fig_timescales.pdf}\\[-2pt]
{\footnotesize\textbf{(a) Murray 2014 timescales}}
\end{minipage}\hfill
\begin{minipage}[t]{0.32\linewidth}
\centering
\includegraphics[width=\linewidth]{fig/cogneuro/fig_persistence_betti.pdf}\\[-2pt]
{\footnotesize\textbf{(b) Persistent loops (Gardner 2022)}}
\end{minipage}\hfill
\begin{minipage}[t]{0.32\linewidth}
\centering
\includegraphics[width=\linewidth]{fig/cogneuro/fig_persistence_wasserstein.pdf}\\[-2pt]
{\footnotesize\textbf{(c) Across-condition gap > seed noise}}
\end{minipage}
\caption{\textbf{Three independent cogneuro metrics converge on the blind-vs-sighted dichotomy} (companion to TGM in Fig.~\ref{fig:tgm}). \emph{(a) Murray 2014 intrinsic timescales.} Per-unit single-exponential autocorrelation-decay $\tau$ on PCA-30-reduced $\mathbf{h}_t$, 30 units per condition, fit $R^2 > 0.5$ kept. Boxes: IQR; whiskers: P10--P90 of fitted $\tau$. The \textsc{blind} median $\tau{=}19.3$ lies above the 90th percentile of every sighted condition. The encoder-bandwidth axis reproduces Murray et al.~2014~\citep{murray2014timescales}'s cortical-hierarchy fast-sensor / slow-integrator dichotomy. \emph{(b) Persistent Betti-1 counts (Gardner 2022~\citep{gardner2022toroidal} precedent).} Vietoris--Rips persistence diagrams on 1000-point $\mathbf{h}_t$ subsamples (PCA-32 cosine distance, 3 seeds, persistence threshold $0.1$). Loop counts order monotonically with encoder bandwidth (\uncertain{opposite direction from pre-registration; we report what we found}). Mechanistic reading: blind path-integration revisits aliased states under sensory degeneracy, creating short-lived loops; uniform's visual signal tags each location uniquely, producing a tree-like manifold with fewer cycles. \emph{(c) Across-condition Wasserstein-2 (dgm$_1$) gap dominates within-condition seed noise.} Mean across-pair distance $\approx 17.6$, mean within-condition seed-to-seed $\approx 9.4$; conditions are topologically distinguishable, a parameter-free format claim independent of any probe choice.}
\label{fig:cogneuro_battery}
\end{figure}

\subsection{Consumption axis: probe-readability dissociates from policy reliance}
\label{sec:dissociation}

Probe-readable position indexes \emph{where capacity is allocated}; whether the policy actually uses it is a separate question. \emph{Shortcut discovery}~\citep{tolman1948cognitive} (paired same-scene-different-goal episodes; SPL drop under reset vs.\ persistent LSTM) reveals two off-diagonal cases against probe-readable top-layer GPS (Figure~\ref{fig:consumption_summary}a): \emph{coarse} carries a linear GPS code yet has a low shortcut SPL drop (``READABLE--UNUSED'' --- the policy under-weights the readable GPS); \emph{uniform} has no linear GPS yet has the largest shortcut SPL drop, suggesting the policy reads a non-spatial substitute (``UNREADABLE--USED''). A direct readout test is consistent: a multinomial scene-id classifier on $\mathbf{h}_2$ separates scenes substantially better in uniform than in blind, while within-scene linear position decoding is the other way round --- uniform's $\mathbf{h}_2$ appears to encode scene identity relatively more than position, the inverse of blind. Trajectory-level analysis (Figures~\ref{fig:consumption_summary}b, \ref{fig:shortcut_paired_traj}) gives a candidate mechanism: only uniform's persistent agent ends up closer to the \emph{previous} episode's goal than the new one; \uncertain{a visual / scene-history anchor (``head to where you were going last episode'') is one consistent reading.}

\begin{figure}[!t]
\centering
\begin{minipage}[t]{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{fig/cogneuro/fig_consumption_scatter.pdf}\\[-2pt]
{\footnotesize\textbf{(a) Probe-readability $\neq$ policy reliance}}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{fig/cogneuro/fig_consumption_margin.pdf}\\[-2pt]
{\footnotesize\textbf{(b) Uniform persistent agent goes to OLD goal}}
\end{minipage}
\caption{\textbf{Consumption-axis dissociation: probe-readable position is not what drives behaviour.} \emph{(a) Off-diagonal cases.} Per-condition shortcut SPL drop (persistent vs reset LSTM, paired same-scene-different-goal episodes; $y$-axis = policy reliance) plotted against linear GPS $R^2$ on $\mathbf{h}_2$ ($x$-axis = probe-readable position). Two off-diagonal conditions break the assumed probe$\to$policy mapping: \textsc{coarse} sits in ``READABLE--UNUSED'' (linear GPS available but ignored by policy), \textsc{uniform} in ``UNREADABLE--USED'' (no linear GPS yet largest behavioural reliance on persistent memory). \emph{(b) The substitute uniform reads.} End-trajectory margin = avg dist(persistent end $\to$ old goal) $-$ avg dist(persistent end $\to$ new goal) on same-floor failures (reset SPL $>0.5$, persistent SPL $<0.2$, persistent steps $\geq 30$, $|\Delta y_{\text{goal}}|<0.5$\,m). Negative = closer to NEW goal; positive = closer to OLD goal. Only \textsc{uniform} ends \emph{closer to the previous episode's goal} ($+1.83$m, $n{=}46$); \textsc{blind}, \textsc{coarse}, \textsc{foveated} all end closer to the new goal ($-0.38$ to $-0.59$m). The substitute is a visual / scene-history anchor: ``head to where you were going last episode.'' Canonical per-condition trajectory examples: Figure~\ref{fig:shortcut_paired_traj}; full $4{\times}4$ catalog: Appendix~\ref{app:shortcut-catalog}.}
\label{fig:consumption_summary}
\end{figure}

\subsection{Causal intervention: memory-init transplant and GPS-sensor ablation}
\label{sec:causal-intervention}

\noindent Two causal stress-tests provide behaviour-level evidence (Figure~\ref{fig:behavioral}). \emph{Memory-init transplant} (Figure~\ref{fig:behavioral}a; re-initialise the LSTM from previous-episode end-memory vs.\ zero): all five conditions show a comparable SPL drop. The cross-condition uniformity is itself the finding: the policy at $t{=}0$ expects a zero hidden state regardless of encoder type, consistent with $\mathbf{h}_2$ acting as a dynamics-only \emph{activity-slot} memory rather than a synaptic store~\citep{dorrell2024taleoftwo}. \emph{GPS-sensor ablation} (Figure~\ref{fig:behavioral}b; zero GPS$+$compass from step 0) collapses all five conditions to near-zero success: GPS appears causally important across the spectrum. The absolute drops cluster tightly across the four sighted conditions and do not cleanly track the dissociation direction; we therefore report this as a check on GPS causality rather than as independent ranking-level evidence for the integration-vs-substitute split established above. Boundary tests, classifier protocol, and the failed mid-rollout visual ablation are detailed in Appendix~\ref{app:probing-details}.

\begin{figure}[!t]
\centering
\begin{minipage}[t]{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{fig/cogneuro/fig_memory_init.pdf}\\[-2pt]
{\footnotesize\textbf{(a) Memory-init transplant}}
\end{minipage}\hfill
\begin{minipage}[t]{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{fig/cogneuro/fig_gps_ablation.pdf}\\[-2pt]
{\footnotesize\textbf{(b) GPS+compass ablation}}
\end{minipage}
\caption{\textbf{Causal interventions: $\mathbf{h}_2$ is a trajectory-bound activity-slot memory, and GPS is causally critical across all conditions.} \emph{(a) Memory-init transplant} ($n{=}150$ ep/cond): re-initialising the LSTM hidden state from the previous-episode end-memory (instead of the trained-for zero state) drops SPL by $0.10$--$0.14$ \emph{across all five conditions}. \textsc{Blind}'s exact value ($-0.14$, $0.81 \to 0.67$) is the published anchor; other conditions are within the published $[-0.14, -0.10]$ band (hatched bars; per-condition exact values are not preserved in the result file). The cross-condition uniformity is the finding: every agent's policy at $t{=}0$ expects a zero hidden state, so the recurrent code is dynamics-only (\emph{activity-slot} memory~\citep{dorrell2024taleoftwo}), not a synaptic store. \emph{(b) GPS+compass ablation:} zeroing GPS and compass from step 0 collapses success to $0$--$6\%$ across all conditions (red arrows = per-condition success drop; baseline grey, ablation coloured). The absolute drops cluster tightly across the four sighted conditions ($-0.92$ to $-0.97$); blind drops less ($-0.75$) because its baseline is already lower. Together (a) and (b) establish the \emph{behavioural} consequences of the capacity-allocation principle: capacity is dynamics-bound (memory-init breaks all conditions equally), and the GPS sensor is the upstream causal source whose integration the recurrent state encodes (zeroing it breaks behaviour everywhere).}
\label{fig:behavioral}
\end{figure}


\FloatBarrier
\section{Discussion}
\label{sec:discussion}

\subsection{Synthesis: three axes of one capacity allocation}

\begin{figure}[!t]
\centering
\includegraphics[width=0.65\linewidth]{fig/fig6_synthesis_2axes.pdf}
\caption{Synthesis: three axes of one capacity allocation. \emph{X-axis:} top-layer GPS $R^2$ on $\mathbf{h}_2$ (magnitude). \emph{Y-axis:} $-1\times$ average cross-condition transplant cost when this condition is the donor (format-isolation; higher Y = more disruptive to other conditions' policies). \emph{Marker size:} second-episode shortcut SPL drop $\%$ (consumption; larger = more policy dependence on persistent recurrent memory). Within each axis, multiple convergent measurements --- e.g.\ information-allocation, scene-invariance, predictive-horizon, place-cell, and excursion-fragility findings (Figures~\ref{fig:format_shift}, \ref{fig:loso_cv}, \ref{fig:place_cells}) --- sharpen the \emph{interpretation} but are not separate axes of variation.}
\label{fig:synthesis_2axes}
\end{figure}

The three axes cohere as views of one capacity allocation (Figure~\ref{fig:synthesis_2axes}). \emph{Magnitude} (X): bottleneck conditions retain a linear top-layer GPS code, rich-encoder ones reallocate to the visual route (§\ref{sec:h1}); within-§\ref{sec:h1} measurements (information allocation $\approx 1.3\times$ MINE shift, predictive horizon to $k=20$ for blind, place-unit count, $+0.381$ $\Delta$MAE excursion fragility) sharpen what the axis means without adding axes. \emph{Format} (Y): within-condition (LOSO scene-invariance dichotomy, Figure~\ref{fig:loso_cv}) and across-condition (asymmetric $5{\times}5$ memory transplant, catastrophic cross-condition probe transfer, and near-orthogonal subspaces, §\ref{sec:h2}) views combine to show subspaces are jointly incompatible at the readout level. \emph{Consumption} (marker size): a bottleneck-encoder agent under-weights its readable code while a rich-encoder agent reads a non-spatial substitute (scene history) (§\ref{sec:dissociation}). Each condition occupies a distinct $(X, Y, \text{size})$ coordinate, with the bandwidth--allocation tradeoff one of three orthogonal axes, not the whole picture. \emph{Foveation occupies a hybrid position}: it shares uniform's rich-encoder magnitude but separates from uniform on every other measure --- most strikingly, foveated agents recover a held-out-MP3D GPS code that uniform agents do not ($R^2 = +0.35$ vs.\ $-0.36$, a $0.71$-magnitude transferability gap). Foveated--uniform pair similarity ($\theta_1 = 1.08$, kernel-CKA $0.035$) is the closest cross-condition pairing in our matrix yet still highly distinct, consistent with the visual and integration routes being independent capacity sinks rather than two ends of one knob.

\uncertain{The capacity-allocation principle plays out differently in coarse and uniform even though both lie on the magnitude axis: coarse's encoder cannot supply spatially-resolved features, so capacity is allocated to integrating the GPS-sensor signal; uniform's encoder supplies rich features and capacity is reallocated to the visual route, leaving the integrated GPS code unattended in $\mathbf{h}_2$. The two are not the same mechanism, only two regimes of one allocation.}
\subsection{Biological precedent and predictions}
\label{sec:bio}

The inline parallels at §\ref{sec:h1}--§\ref{sec:dissociation} (rodent dark-rearing~\citep{chen2016grid}, hidden-state-inference remapping~\citep{sanders2020remapping}, bat cross-modality remapping~\citep{geva2016remapping}, activity-slot memory~\citep{dorrell2024taleoftwo}) place our results within sensor-niche-shapes-spatial-memory, a hypothesis with long-standing comparative-cognition precedent~\citep{rolls2024why,fortin2008blind,kupers2014blind}. We do not claim our silicon results \emph{demonstrate} the bio-AI parallel, only that the patterns we observe under controlled within-architecture variation are at high level consistent with what one would expect if the hypothesis held in animals. Loosely mapped, our \emph{blind} condition is closest to subterranean rodents~\citep{kimchi2004molerat}; \emph{coarse} to single-channel sensory summaries (echolocation, infrared pit-detection); \emph{foveated} to the primate / raptor regime (gaze fixed; gaze placement is a future-work axis); \emph{uniform} to insect compound eye / zebrafish near-isotropic sampling~\citep{geva2015spatial,toledo2020cognitive} --- interpretive scaffolding rather than mechanistic identity claims.

\noindent\textbf{Predictions back into biology.}\quad Under \citet{sanders2020remapping}'s hidden-state-inference framework, place fields remap when sensory evidence supports distinct hidden states. Our LOSO catastrophe in rich-encoder conditions exactly matches this regime (rich per-step visual evidence supports scene-specific hidden states; the linear position direction reorganises across rooms, analogous to global remapping); bottleneck conditions exhibit no remapping (a single scene-invariant code shared across contexts). Two further bio-AI bridges follow from §\ref{sec:tgm}: the \textsc{blind} agent's \emph{transient-code} TGM signature mirrors the diagonal-only patterns King \& Dehaene 2014 originally identified in MEG decoding of working-memory recompute regimes; and the $\sim 2{.}1{\times}$ longer intrinsic timescale we measure in \textsc{blind} reproduces the Murray 2014 cortical-hierarchy fast-sensor / slow-integrator dichotomy on the encoder-bandwidth axis (\textsc{blind} as cortex's slow-integrator pole, sighted-rich conditions as the fast-sensor pole). Both of these are ``the same metric one would run on monkey or human cortex, applied unchanged to a recurrent navigator''. Three falsifiable predictions follow. \emph{(i)} Within-species manipulations changing effective per-step visual variety (gaze restriction, dark-rearing, prosthetic compound vision) should shift hippocampal coding along the no-remapping vs.\ global-remapping axis. \emph{(ii)} Gaze-fixed primates should show coding closer to rodent place cells than to free-gaze primate view cells; \citet{wirth2017gaze}'s primate hippocampal recording under restricted gaze provides the closest existing dataset, and a within-animal contrast between free and restricted gaze would directly test it. \emph{(iii)} The Skaggs, LOSO, predictive-horizon, MINE, and subspace-divergence findings cohere under one allocation rather than five separate phenomena --- cognitive map theory has catalogued such phenomena separately~\citep{tolman1948cognitive,okeefenadel1978hippocampus,stachenfeld2017predictive,whittington2020tem}, and our setting suggests they may be views of a single mechanism. The structural-constraint reading complements recent results that emergent grid-cell modular structure itself depends on local-interaction priors rather than the path-integration objective alone~\citep{khona2025peakselection,schaeffer2022nofreelunch}. Whether biological hippocampi obey the finite-capacity allocation principle remains an open empirical question.

\subsection{Implications and scope}

\noindent\textbf{IB framing and capacity-bound sketch.}\quad The capacity-allocation principle aligns with the invariance--disentanglement reading of the information bottleneck~\citep{tishby2000ib,achille2018invariance}: bandwidth-restricted memory is the architectural mechanism that drives the trained representation toward scene-invariant rather than scene-conditional spatial encoding (Figure~\ref{fig:loso_cv}). The framing has a quantitative skeleton: a hidden state with effective rank $d$ allocates dimensions among (i)~directly carrying encoder-derived features (capacity $d_e$), (ii)~integrating sensor history (capacity $d_i$), and (iii)~carrying task-relevant non-position content (capacity $d_o$), subject to $d_e + d_i + d_o \lesssim d$. Bottleneck encoders force $d_e \to 0$, leaving $d_i$ to dominate the linear position code; rich encoders enlarge $d_e$, so policy gradient minimises $d_i$ down to whatever residual the integration route still needs. Our Procrustes / participation-ratio / eigenspectrum-$\alpha$ measurements collectively probe $d$ (effective rank $\approx 4$ for blind, lower for coarse where the encoder collapse shrinks $d$ overall, Appendix~\ref{app:eigenspectrum}); the magnitude-axis collapse follows from $d_i$ shrinking as $d_e$ grows. \uncertain{We use IB descriptively rather than mechanistically; \citet{saxe2019information}'s critique of IB-deep-learning theory cautions against absolute MI-magnitude claims, but the cross-condition \emph{ordering} reported here (linear collapse, MLP recovery, MINE rank in Appendix~\ref{app:mine}) is robust to those concerns.}

\noindent\textbf{Falsifiable predictions and broader implications.}\quad The capacity-allocation principle is architecture-agnostic: it predicts a content-level allocation in any system where a finite-capacity memory module reads a pretrained encoder. For transformer-based PointGoal navigators (e.g.\ \citealt{zeng2024poliformer}; cf.\ \citealt{whittington2022transformerhippocampus} for the transformer/hippocampus correspondence), where ``memory'' lives in the KV cache or attention over rollout history, the principle predicts the same monotonic anti-correlation reported in Figure~\ref{fig:magnitude}a: attention-readable position codes under bandwidth-restricted encoders, attention-internal position codes only non-linearly decodable under rich encoders. The cleanest falsification target is direct: train a transformer navigator under our 5 sensor conditions; observing the opposite pattern would falsify the principle's architecture-independence and bound it to recurrent-state systems. The same logic re-frames recent VLM spatial-reasoning failures~\citep{ramakrishnan2025space}: rich pretrained encoders may divert capacity from the downstream substrate's spatial code rather than merely fail to teach it, and bandwidth-restricting perception (foveation, log-polar) may be \emph{cognition-enabling} rather than only compute-saving. The relevant axis is encoder spatial-feature \emph{variety} per step rather than raw input bandwidth (Appendix~\ref{app:scaling}); a finer multi-point sweep within recurrent agents is left to future work.

\noindent\textbf{Architecture-agnostic confirmation: format shift reproduces in a frozen encoder with a small LSTM stack on a different environment (Figure~\ref{fig:wmprobe}).}\quad As a partial test of the architecture-independence claim, we ported the encoder-bandwidth axis to a different environment, encoder family, and training regime: frozen DINOv2-Base~\citep{oquab2024dinov2} patch features at five resolutions (mirroring our Habitat chassis: \textsc{blind}, \textsc{coarse}, \textsc{foveated}, \textsc{uniform}, \textsc{foveated-logpolar}) feed a small 2-layer LSTM trained on Memory Maze~\citep{pasukonis2023memorymaze} 9$\times$9 offline trajectories with next-feature prediction. We then probe \texttt{agent\_pos} from the LSTM hidden state (Ridge linear and 4-layer MLP, frame-level CV with 80/20 random split, eval window steps 250--500; full pre-registration in \texttt{scripts/probing/world\_model\_probe/PRE\_REGISTRATION.md}). Three pre-registered conditions land cleanly. (1) Linear $R^2$ is monotone-increasing with encoder bandwidth: \textsc{blind} $0.01$, \textsc{coarse} $0.38$, \textsc{foveated} $0.41$, \textsc{foveated-logpolar} $0.41$, \textsc{uniform} $0.45$. (2) Non-linear $R^2$ (4-layer MLP) is uniformly high in all sighted conditions ($\approx 0.998$) and at chance for \textsc{blind} ($-0.0003$). (3) The non-linear gap (MLP$-$linear) is monotone-\emph{decreasing} with bandwidth: \textsc{coarse} $+0.62$, \textsc{foveated} $+0.59$, \textsc{foveated-logpolar} $+0.59$, \textsc{uniform} $+0.55$ --- format-shift recovery is largest where linear readability is smallest, exactly the dichotomy our Habitat result reports. \emph{Direction relative to Habitat.} The Memory-Maze setup deliberately omits the GPS/compass channel our Habitat agents receive; without that explicit positional anchor, the encoder is the only sensory route to absolute position, and \textsc{blind} cannot decode it (no signal, no integration anchor). The bandwidth-allocation \emph{tradeoff} (where blind's integration route compensates) thus requires a competing route; the format-shift dynamic (linear-readable code emerges from a non-linear substrate as bandwidth grows) generalises across both regimes. The two together place the broad ``encoder structure shapes memory format'' claim on a different environment, encoder family, and policy regime, while clarifying that the specific direction (high-bandwidth-rich vs.\ bottleneck-rich) is mediated by which sensory channels carry positional information.

\begin{figure}[!t]
  \centering
  \includegraphics[width=\linewidth]{fig/cogneuro/fig_world_model_probe.pdf}
  \caption{\textbf{Architecture-agnostic confirmation: format shift reproduces on Memory Maze with a frozen DINOv2-Base encoder feeding a small LSTM.} \emph{(A)} Linear (Ridge, best of $\alpha \in \{0.1, 1, 10, 100, 1000\}$, blue) and 4-layer MLP (red) eval $R^2$ for \texttt{agent\_pos} from the LSTM hidden state, frame-level random CV (80/20), eval window steps 250--500 of $T{=}500$ Memory-Maze trajectories. Linear $R^2$ is monotone-increasing with encoder bandwidth (\textsc{blind} $0.01$ $\to$ \textsc{uniform} $0.45$); MLP $R^2$ saturates at $\approx 0.998$ for all sighted conditions and is at chance for \textsc{blind}. \emph{(B)} The non-linear gap (MLP$-$linear) is monotone-\emph{decreasing} with bandwidth: format-shift recovery is largest where linear readability is smallest (\textsc{coarse} $+0.62$ vs.\ \textsc{uniform} $+0.55$), the same dichotomy our Habitat agent shows in Figure~\ref{fig:format_shift}. The opposite linear-$R^2$ direction relative to Habitat reflects the absence of GPS/compass channels in the Memory-Maze setup --- without an alternative positional anchor, the encoder is the only route, and \textsc{blind} carries no signal.}
  \label{fig:wmprobe}
\end{figure}

\subsection{Limitations}
\label{sec:limitations}

(i)~\emph{Single seed per condition}, following the comparative-cognition modelling convention~\citep{wijmans2023emergence,banino2018grid,cueva2018grid,schaeffer2023grid}. Cross-condition rigour comes from convergent multi-method evidence (linear and MLP probing, CKA, Procrustes, subspace divergence, LOSO, lag-$k$, $5{\times}5$ transplant, Skaggs, excursion-forgetting all rank the conditions in the same direction --- unlikely under single-seed noise alone), supplemented by an independent-retraining replication of the blind condition (Appendix~\ref{app:repro}). Per-condition seed-variance error bars over a multi-seed grid are reported as future work. (ii)~\emph{Method-family and content-axis scope.} Methods we do not report and that could change a finding's strength: dynamical-systems analysis (fixed-point/slow-point structure~\citep{sussillo2013opening}; Dynamical Similarity Analysis~\citep{ostrow2023dsa} as a dynamics-aware Procrustes alternative) would speak to the format axis at the level of computational primitives. We piloted DSA on our $h_t$ time-series (10-d PCA pre-reduction, $50$-step trajectories) and found the cross-condition signal sensitive to HP (n\_delays, rank), rollout protocol (excursion-warmup vs.\ deterministic-rollout), and episode-phase alignment (blind has median episode length $\sim 2.5{\times}$ sighted, so first-$T$-step samples occupy different behavioural phases) at our explored settings; the 5-condition pattern under the cleanest protocol (deterministic-rollout, all conditions, $T{=}50$) showed sighted clustering at floor $\sim 0.10$ angular while blind was distant ($\sim 0.27$), but blind's within-condition split-half DSA was also $\sim 0.23$, leaving the SNR insufficient to publish a definitive ranking. Information-theoretic estimators of $I(h_t; \text{pos}_{t+k})$ would replace decodability $R^2$ with predictive-horizon nats; gaze placement and a $\sigma_{\max}$ strength sweep within the foveated condition test the rich-encoder content axis. All four are left for follow-up. (iii)~\emph{Architecture, algorithm, and task scope.} We use a recurrent (LSTM) backbone with DD-PPO, PointGoal, and a ResNet-18 visual encoder, matching the canonical pipeline of \citep{wijmans2023emergence,banino2018grid,cueva2018grid} for direct comparability; the architecture choice is replication-driven rather than central to the claim, and the principle is stated at the encoder-memory interface (architecture-agnostic). A reasonable concern is whether the encoder--memory race is an LSTM artefact (e.g.\ vanishing-gradient gating limitations); two within-paper checks argue against it: 2-layer MLP probes recover position in rich-encoder conditions ($R^2 \in [0.51, 0.73]$), so the LSTM \emph{does not} discard position --- the linear-readable axis tracks encoder bandwidth, a property of the linear DD-PPO action head; and the cross-condition memory transplant (§\ref{sec:h2}) is a behavioural test outside any probe framework that reproduces the same ordering. The encoder--memory race is in principle architecture-agnostic; the cleanest falsification target is a transformer-based navigator under the same conditions, identified in §\ref{sec:discussion} but not verified here. The magnitude- and format-axis patterns could also shift under different RL algorithms, different navigation tasks (ObjectGoal, ImageGoal, exploration), or different visual encoders.


\section{Conclusion}

A five-condition visual-sensor ablation in a recurrent PointNav agent shows sensor structure as a representational-content axis whose effects cohere under a single capacity-allocation principle measured along three axes: \emph{magnitude} (bandwidth--allocation tradeoff between encoder and recurrent-integration routes), \emph{format} (within-condition scene-invariance, across-condition non-interchangeable subspaces with asymmetric memory transplant, and a King--Dehaene-style temporal-format dissociation in which the blind code rotates over time while the coarse code stabilises), and \emph{consumption} (probe-readable position dissociates from policy reliance). The bandwidth--allocation ordering is preserved on held-out MP3D, blind units integrate over a $2.1{-}2.5{\times}$ longer Murray-cortical-hierarchy timescale than every sighted condition, and the pattern is at high level consistent with independent animal-navigation findings on hippocampal compensation under sensory deprivation, cross-modality remapping, and the no-/global-remapping axis under hidden-state inference~\citep{sanders2020remapping} --- framed as an open bio--AI hypothesis rather than a demonstrated parallel.

\noindent Methodologically, we view the paper as a step toward a \emph{comparative cognitive neuroscience of artificial navigation agents}: a research programme in which matched-condition populations of trained agents are analysed with the same set of metrics --- population geometry, intrinsic timescales, dynamical fixed points, predictive-coding residuals, persistent-homology of the state manifold, mixed-selectivity / splitter analyses, communication subspaces, transfer entropy --- that is used to map biological cortex and hippocampus. We piloted this analytic suite on the present sensor axis (Figures~\ref{fig:tgm}, \ref{fig:cogneuro_battery}, \ref{fig:wmprobe}): temporal-axis methods (TGM, Murray timescales) and topological methods (persistent homology) detect strong cross-condition signal aligned with capacity allocation, and the format-shift dynamic itself reproduces in a frozen encoder feeding a small LSTM on a different environment. Beyond this paper, the same suite can be applied to architecture-axis populations (recurrent vs.\ transformer world models), training-curriculum axes (sparse vs.\ dense reward), or biological-vs-silicon axes (LSTMs trained on egocentric video versus rodent CA1 recordings under matched stimuli). The goal is not to claim that artificial agents \emph{are} brains, but that the analytical methods used to ask ``how does this neural system represent its world?'' transfer to artificial systems where the relevant variables can be held fixed --- and that the cross-condition contrasts produce theoretically informative signatures (capacity allocation, format shifts, dynamical regimes) of the same kind comparative neuroscience uses to understand sensor-niche adaptation in animals. The encoder--memory race is stated as an interface-level claim and should in principle generalise to transformer-based navigators (the cleanest falsification target, §\ref{sec:discussion}). Cross-condition rigour rests on multi-method convergence and an independent-retraining blind replication (Appendix~\ref{app:repro}); a multi-seed grid, finer encoder-bandwidth sweep, and gaze-placement controls are reported as future work.

\paragraph{Code and checkpoints.} Code is available at \url{https://github.com/alunxu/foveated-cog-map}; the four canonical converged checkpoints plus intermediate cross-training checkpoints (used for the §\ref{sec:h1} substitution-dynamics figure) are hosted at \url{https://huggingface.co/alunxu/spatial-memory-checkpoints}.


\bibliographystyle{plainnat}
\bibliography{literature}

\newpage
\input{checklist}

\newpage
\appendix

\section{Reproducibility: hyperparameters and implementation notes}
\label{app:repro}

\noindent\textbf{Training hyperparameters (DD-PPO, all five conditions identical)}\quad
4-GPU \texttt{torchrun}; \texttt{num\_steps=256}, learning rate \texttt{lr=2.5e-4} with \texttt{lr\_decay=False}; \texttt{num\_environments=16/worker}; total budget 250M frames per condition. All other hyperparameters follow the Habitat-baselines DD-PPO defaults from \citet{wijmans2020ddppo}.

\noindent\textbf{Architectural details (sensor encoding)}\quad
Each non-visual sensor (goal-in-start-frame, GPS, compass, close-to-goal scalar) is projected to 32-d via a per-sensor linear layer; the previous-action embedding is also 32-d (\texttt{nn.Embedding} with action-space cardinality + 1 for the no-prev-action token). The four sensor projections are concatenated with the previous-action embedding and the (flattened) visual encoder output to form the LSTM Layer-0 input vector. This matches the \citet{wijmans2020ddppo} Wijmans-default sensor stack.

\noindent\textbf{Implementation notes (foveated condition)}\quad
The foveated condition disables the running-mean-and-variance input normaliser (\texttt{RunningMeanAndVar}) to avoid an in-place autograd conflict between the running-buffer update and the foveation-blur backward pass. Behavioural success and SPL are unaffected; the ablation is in the codebase under \texttt{src/habitat/foveated\_normalised\_policy.py} (normaliser \emph{enabled}) for future work that wants to revisit this choice.

\noindent\textbf{Implementation notes (numerical stability)}\quad
Two safeguards protect against rare numerical instabilities arising from off-navmesh episodes: (i)~gradient sanitisation in \texttt{wijmans\_policy.\_safe\_before\_step} replaces NaN/Inf gradients with zero before each PPO update; (ii)~a forward-looking \texttt{geodesic\_distance} clamp in \texttt{nan\_safe\_measures.py} prevents reward NaNs when the agent steps off the navmesh. Both are no-ops on healthy trajectories.

\noindent\textbf{Log-polar foveation transform}\quad
The log-polar control (Appendix~\ref{app:scaling}) uses \texttt{LogPolarFoveationTransform} in \texttt{src/habitat/torch\_foveation.py}: resample the $128{\times}128$ avg-pooled image onto a $64{\times}64$ ($n_\rho \times n_\theta$) log-polar grid before the ResNet-18 backbone. Visual front-end only; LSTM, sensors, and training pipeline are identical to the foveated condition.

\section{Probing protocol details}
\label{app:probing-details}

\noindent\textbf{Sampling protocol}\quad An earlier draft used stochastic (policy-distribution) action selection at probe time. Conditions with higher action-entropy under their trained stochastic policies sampled the STOP action early with $\sim 0.25$ per-step probability, producing $\sim 4$-step rollouts and target variance two orders of magnitude below the episodic range. The resulting probe $R^2$ values were trivially high across all conditions (any predictor fits a near-constant target). This confounded cross-condition comparisons; we switched to the deterministic (argmax) protocol used here and elsewhere in our pipeline. All paper results use the deterministic protocol.

\emph{Length-matching ablation.} Under deterministic rollouts, episode lengths span $\sim$100--400 mean steps depending on condition. We use 5-fold episode-level CV to keep splits balanced per scene rather than length-matching across conditions. A controlled length-matching ablation (truncate every condition to a common length) is left for follow-up; we expect the H1 ordering to be preserved because bottleneck conditions decode at high $R^2$ even at the smallest sub-sample, but have not verified.

\emph{Cross-heading probe.} The proposal included a cross-heading generalisation probe (train on heading $A$, test on opposite heading). Our implementation returned ``insufficient samples'' sentinels at our probe sizes; not in our results.

\section{Probing details (Layer 0 sensor branch and MLP probe)}
\label{app:probe-details}

\emph{LSTM Layer 0 sensor branch.} The non-visual sensor stack (goal-in-start-frame, GPS, compass, close-to-goal scalar, prev-action) projects each sensor to 32-d, concatenates them with the visual encoder's flattened output, and feeds the result as the LSTM's Layer 0 input. The GPS embedding therefore enters the LSTM via this concatenation; this explains why Figure~\ref{fig:magnitude}c shows linearly-decodable GPS at Layer 0 even for conditions whose visual encoder discards GPS entirely.

\emph{MLP probe details.} For each condition we fit a 2-layer MLP (hidden dim 256, ReLU, $L_2$ weight decay $10^{-4}$) on the same top-layer hidden states and GPS labels as the linear Ridge probe, with the same 5-fold episode-level CV. Top-layer recovery: foveated $R^2 = 0.51$, uniform $0.55$ (vs.\ Ridge's chance R²). The MLP probe alone cannot disentangle ``non-linear position code'' from ``MLP regressing through position-correlated features (wall distance, scene identifiers, episode-phase markers)''; the log-polar foveation control (Appendix~\ref{app:scaling}) targets the encoder-spatial-output axis at one intermediate point.

\section{Alternative-account rebuttals (full)}
\label{app:alt-accounts}

\emph{Task-competence differences.} All conditions solve PointGoal at $93$--$99\%$ success (Table~\ref{tab:summary}); the encoded-content gap cannot be attributed to navigation skill.

\emph{Probe-capacity / probe-fitting artefacts.} DtG decodes robustly across all four conditions ($R^2 \geq 0.81$), and the real-vs-permuted-label probe gap (Hewitt--Liang selectivity) tracks GPS $R^2$; the chance-level rich-encoder GPS $R^2$ is therefore a hidden-state property, not a probe artefact.

\emph{Sample-size / trajectory-distribution artefacts.} CV $\sigma$ spans come from the same 500-episode pool with $\sim 100$--$400$-step rollouts under identical deterministic evaluation; the protocol confound that haunted an earlier draft is documented and retired in Appendix~\ref{app:probing-details}.

\emph{Linear-probe artefacts.} Two complementary checks. \emph{(a)~Behavioural:} the memory-transplant intervention goes outside the probe framework and reproduces the bottleneck-vs-rich-encoder ordering; the $5\times 5$ matrix's asymmetry adds structure no linear-similarity measure can read. \emph{(b)~Non-linear probe:} a 2-layer MLP recovers $R^2 \in [0.51, 0.73]$ from rich-encoder top-layer states (Appendix~\ref{app:probe-details}). H1 is therefore specifically about \emph{linear} top-layer readability --- what the linear DD-PPO action head can consume --- not GPS absence.


\section{Supplementary visualisations}
\label{app:supp_figs}

\begin{figure}[!t]
\centering
\begin{minipage}{0.52\linewidth}\centering
\includegraphics[width=\linewidth]{fig/appfig7_training_curves.pdf}\\
{\footnotesize (a) Training curves (TensorBoard event-file scalars). All three sighted conditions converge to SPL $0.82$--$0.85$ and success $>97\%$ by $\sim$100M frames; blind converges to SPL $\sim$0.50 over a longer horizon.}
\end{minipage}\hfill
\begin{minipage}{0.44\linewidth}\centering
\includegraphics[width=\linewidth]{fig/appfig7_h2_hidden_embedding_tsne.pdf}\\
{\footnotesize (b) t-SNE projection of top-layer LSTM hidden states (deterministic-rollout probes), pooled across all four conditions (perplexity 30). The four conditions occupy non-overlapping regions of the 2-D embedding --- a visual confirmation of the near-zero CKA result (§\ref{sec:h2}).}
\end{minipage}
\caption{Supplementary visualisations referenced from the main text.}
\label{fig:supp_figs}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.55\linewidth]{fig/appfig8_h2_cka_heatmap.pdf}
\caption{Unaligned linear CKA~\citep{kornblith2019cka} between top-layer LSTM hidden states ($n{=}95{,}640$ steps per condition under deterministic-rollout probes); every off-diagonal is below $10^{-4}$. Within-condition split-half CKA $\approx 1$ confirms the near-zero off-diagonals are not estimator noise. Pairs that look identical here differ markedly under transplant and probe-transfer (Figure~\ref{fig:h2}); CKA is the least informative of the three H2 measures because it cannot read the asymmetric structure that transplant and probe-transfer show.}
\label{fig:supp_cka}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.95\linewidth]{fig/appfig9_transplant_sweep.pdf}
\caption{Memory-transplant midpoint sweep on two representative donor$\to$recipient pairs (foveated$\to$uniform, foveated$\to$blind) across midpoints $\{0, 15, 30, 60, 90, 200, 400, 800\}$ time-steps ($n{=}100$--$150$ episodes/cell). The full $5{\times}5$ matrix in Figure~\ref{fig:h2} (right) is at midpoint $t{=}30$. \emph{(a):} full SPL per condition --- baseline / self / cross. The $t{=}0$ self/cross collapse to a protocol-noise floor (so the diagonal in Figure~\ref{fig:h2} is correctly read as rollout-divergence noise, not condition mismatch); SPL decays for all conditions at $t \geq 200$ because by then most successful episodes have already terminated, leaving only long atypical episodes. \emph{(b):} cross-minus-self isolates condition-mismatch alone. The gap peaks in the $t \in [30, 90]$ window --- where the agent has integrated history but is not yet committed to a trajectory --- and decays toward $0$ at $t \geq 400$. The asymmetry pattern reported at $t{=}30$ in Figure~\ref{fig:h2} is therefore neither a $t{=}30$-specific artefact nor a late-midpoint inflation.}
\label{fig:transplant_sweep_supp}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/appfig10_extra_states.pdf}
\caption{Linear probe ($R^2$, 5-fold CV) at each LSTM hidden state $\mathbf{h}_\ell$ and cell state $\mathbf{c}_\ell$, $\ell \in \{0, 1, 2\}$, for all 4 conditions. \emph{Left:} GPS. \emph{Right:} compass. Three patterns. \emph{(i)~At $\mathbf{h}_0$ all conditions decode GPS at $R^2 \sim 0.95$} (the GPS sensor embedding enters the LSTM concat there, regardless of vision condition). \emph{(ii)~$\mathbf{h}_1$ dips for every condition} including blind ($R^2 = +0.22$); information passes through middle layer with reduced linear decodability for everyone, then partly recovers at $\mathbf{h}_2$ for bottleneck. \emph{(iii)~Cell states $\mathbf{c}_\ell$ do \emph{not} carry the GPS code as cleanly as $\mathbf{h}_\ell$}: $\mathbf{c}_1$ is at chance/negative for every condition; $\mathbf{c}_2$ is positive only for blind ($R^2 = +0.57$). The H1 claim about ``top-layer linearly-decodable GPS'' is therefore specifically about $\mathbf{h}_2$ (the policy readout), not the cell-state pathway. Same single-seed limitation as the main paper.}
\label{fig:supp_extra_states}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.95\linewidth]{fig/appfig11_population_coding.pdf}
\caption{Population coding analysis (referenced from §\ref{sec:boundaries}). \emph{(a)}~Per-unit spatial-information distribution: each curve is one condition's units sorted by spatial information (descending), x-axis log scale. Counter-intuitively, rich-encoder conditions (uniform, foveated) have a few more peaked spatially-tuned units (max $\approx 2$ bits) than bottleneck conditions; blind has the flattest distribution (max $\approx 1$ bit, no unit reaches the dashed 1-bit threshold). \emph{(b)}~Sparse-vs-distributed GPS decoding: probe $R^2$ when only the top-$k$ spatial-info units are used as features. Blind reaches $R^2 \approx 1.0$ at $k=512$; coarse climbs to $R^2 \approx 0.5$; rich-encoder networks fail to decode GPS at any $k$ even though their top units carry higher per-unit spatial information. The combined picture: \emph{higher per-unit spatial information $\neq$ position readout}. Rich-encoder peaked units appear to encode features correlated with position (e.g.\ visual landmarks, trajectory phase) rather than position itself.}
\label{fig:supp_pop_coding}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.7\linewidth]{fig/appfig12_goal_vector.pdf}
\caption{Goal-vector probe $R^2$ (referenced from §\ref{sec:boundaries}). Goal \emph{distance} (DtG) decodes strongly across all conditions ($R^2 \in [0.81, 0.90]$); goal \emph{direction} and the full ego-frame goal vector are at chance or negative everywhere. PointGoal provides the goal vector as a per-step input, so the policy has no incentive to re-encode it.}
\label{fig:supp_goal_vec}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/appfig_tsne.pdf}
\caption{t-SNE of top-layer LSTM hidden states $\mathbf{h}_2$, supplementing the linear-similarity H2 results (§\ref{sec:h2}). \emph{Left:} joint t-SNE of 5\,000 samples (1\,000 per condition) in 512-d hidden space. The four conditions form clearly separable clusters in the non-linear embedding, complementing the 1-NN purity ($1.000$ vs.\ chance $0.20$) and pairwise linear CKA ($<10^{-4}$) results: format divergence is robust under both linear and non-linear similarity tests. \emph{Right:} per-condition t-SNE coloured by distance-to-goal ($n=2\,000$ per panel). Within-condition structure varies: blind shows a clearer DtG gradient than the rich-encoder conditions, consistent with the H1 finding that blind's hidden state carries position information more directly while rich-encoder conditions encode it less linearly.}
\label{fig:supp_tsne}
\end{figure}

\subsection{Capacity-allocation: additional geometric tests}
\label{app:cap_allocation_extras}

The three figures below provide independent geometric tests of the capacity-allocation principle, beyond the headline information-allocation (Fig.~\ref{fig:format_shift}) and LOSO scene-invariance (Fig.~\ref{fig:loso_cv}) results in §\ref{sec:h1}.

\begin{figure}[!t]
\centering
\includegraphics[width=0.55\linewidth]{fig/fig_cross_cond_transfer.pdf}
\caption{\textbf{Cross-condition probe-transfer matrix.} Ridge-probe trained on condition $A$'s top-layer states, applied to condition $B$'s. Diagonal (own-probe) $R^2 \in [0.90, 0.99]$. Off-diagonal $R^2$ is \emph{catastrophically negative} ($-3.5{\times}10^2$ to $-3.5{\times}10^4$ unclipped; clipped to $[-1, 1]$ for visualisation): the position-axes encoded across conditions are not just orthogonal in subspace orientation (Figure~\ref{fig:subspace_divergence}) but jointly incompatible at the readout level. Even when the same scenes are visited and the same task is solved, no single linear position-direction transfers between conditions.}
\label{fig:cross_cond_transfer}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.65\linewidth]{fig/fig_position_axis.pdf}
\caption{\textbf{Position-axis lives in low-variance directions of $\mathbf{h}_2$.} Solid: cumulative power of the Ridge-probe singular directions (the linear position-axis) as a function of \# PCs included. Dashed: cumulative explained variance (the manifold's own variance distribution). For all four conditions, the manifold's variance is concentrated in $\sim 7$--$9$ top PCs (90\% by PC 9), but the position-axis distributes its power across 200+ PCs --- it does not reach 50\% within the first 200 PCs. Position information is encoded in the \emph{low-variance tail} of $\mathbf{h}_2$ across all conditions; the discriminator across conditions is therefore not where (in a high-variance sense) position lives but rather the cross-scene stability of the readout direction (Figure~\ref{fig:loso_cv}).}
\label{fig:position_axis}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.55\linewidth]{fig/fig_intrinsic_dim.pdf}
\caption{\textbf{Intrinsic dimensionality (TwoNN).} \citet{facco2017twonn}-style estimate of the intrinsic dimensionality of $\mathbf{h}_2$, on $5\,000$ samples per condition (paper-canonical seed-0 filled markers; seed-2 robustness check hollow). All four conditions yield ID $\approx 2.5$--$2.9$, comparable to the task-intrinsic dimensionality (2-D position $+$ 1-D heading $\approx 3$). The same intrinsic dim across conditions, combined with strongly differing linear $R^2$ and orthogonal subspaces (Figure~\ref{fig:subspace_divergence}), shows capacity-allocation is about the \emph{orientation} of the task-shaped manifold within the 512-d ambient space rather than about its size.}
\label{fig:intrinsic_dim}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/fig_pc_cumulative.pdf}
\caption{\textbf{Linear readout vs.\ \# PCs.} \emph{(a)} Ridge-probe GPS $R^2$ as a function of the number of top PCs of $\mathbf{h}_2$ used as the probe input. Per-cond pattern: blind plateaus quickly even with $\leq 10$ PCs (linear axis aligned with low-variance dirs); rich-encoder conditions barely climb across 50 PCs (information distributed). \emph{(b)} Participation ratio $(\Sigma\lambda)^2/\Sigma\lambda^2$ (number of effectively active dimensions, ignoring tail) across conds. Variance distribution is similarly compact across conditions; what differs is how position information is laid out within these dimensions, supporting the position-axis analysis (Figure~\ref{fig:position_axis}).}
\label{fig:pc_cumulative}
\end{figure}

\subsection{Single-direction $\beta$-scrubbing reveals redundant population coding}
\label{app:scrubbing}

The Ridge probe identifies a $2$-d direction $\beta$ in $\mathbf{h}_2$ that linearly predicts $(x, y)$. We test whether this direction is \emph{causally responsible} for the recoverable position code by projecting $\mathbf{h}_2$ onto the null-space of $\beta$ (rank-$510$ subspace) and re-evaluating both Ridge and MLP probes (Heimersheim \& Nanda 2024 noising-style patching: tests whether $\beta$ was \emph{necessary} for the encoded behavior~\citep{heimersheim2024activation}).

\begin{center}
\begin{tabular}{l|cccc}
\toprule
& Linear orig & Linear scrub & MLP orig & MLP scrub \\
\midrule
Blind   & $+0.95$ & $+0.93$ & $+0.94$ & $+0.95$ \\
Coarse  & $+0.72$ & $+0.62$ & $+0.79$ & $+0.84$ \\
Uniform & $-1.08$ & $-1.46$ & $+0.42$ & $+0.31$ \\
\bottomrule
\end{tabular}
\end{center}

\begin{figure}[!t]
\centering
\includegraphics[width=0.65\linewidth]{fig/fig_subspace_scrubbing.pdf}
\caption{\textbf{$\beta$-subspace scrubbing.} For each cond (foveated deferred), Ridge $\beta$ identifies a 2-d direction in $\mathbf{h}_2$ that linearly predicts (x, y); scrubbed $\mathbf{h}_2 = (I - \beta^+ \beta)\, \mathbf{h}_2$ removes this 2-d subspace. MLP probe is essentially unaffected by scrubbing across all three conditions: position information is redundantly distributed across many h$_2$ dimensions, not concentrated in the 2-d $\beta$ subspace.}
\label{fig:subspace_scrubbing}
\end{figure}

The MLP probe is essentially unaffected by scrubbing across all three conditions ($\Delta R^2 \in [-0.05, +0.11]$). The Ridge probe shows small drops for Blind (-$0.02$) and Coarse (-$0.10$), and a more-negative shift for Uniform (the absolute value increases due to the variance of negative $R^2$ estimates). Two interpretations: (i) the rank-$2$ subspace identified by Ridge is one of many redundant directions encoding position --- when removed, Ridge re-fits to a different direction in the remaining $510$ dims; (ii) MLP, having access to non-linear combinations of remaining dims, trivially recovers the position code from this redundant population. This is the population-coding-redundancy property familiar from biological place-cell systems~\citep{stringer2019high}: single-cell or low-rank ablation does not break the code because of high-dimensional distributed representation. Methodologically, single-direction scrubbing is too weak to causally localise position information in $\mathbf{h}_2$; higher-rank ablation, attribution-based pruning, or path-patching~\citep{heimersheim2024activation} would be next steps. Foveated is deferred to the $250$M re-probe.

\subsection{Predictive horizon: does $\mathbf{h}_t$ encode future positions?}
\label{app:predictive_horizon}

Successor-representation theory~\citep{stachenfeld2017predictive,whittington2020tem} predicts that hippocampal place cells encode not just current position but a discounted future-occupancy code: place-cell firing at $s_t$ corresponds to expected visits to nearby states $s_{t+k}$. We test the analog claim for our recurrent state: does a linear or MLP probe from $\mathbf{h}_t$ recover \emph{future} positions $(x, y)_{t+k}$ for $k \in \{0, 1, 5, 10, 20, 50\}$?

\begin{center}
\begin{tabular}{l|cccccc}
\toprule
 & $k=0$ & $k=1$ & $k=5$ & $k=10$ & $k=20$ & $k=50$ \\
\midrule
\multicolumn{7}{l}{\emph{Linear (Ridge $\alpha = 10$) $R^2$, all-episode pooling, episode-level 5-fold CV}} \\
Blind             & $+0.94$ & $+0.94$ & $+0.94$ & $+0.92$ & $+0.90$ & $+0.79$ \\
Coarse            & $+0.57$ & $+0.57$ & $+0.57$ & $+0.56$ & $+0.50$ & $+0.37$ \\
Foveated          & $+0.06$ & $+0.06$ & $+0.08$ & $+0.13$ & $+0.19$ & $+0.06$ \\
Foveated-logpolar & $+0.17$ & $+0.18$ & $+0.17$ & $+0.11$ & $-0.14$ & $-0.50$ \\
Uniform           & $-4.31$ & $-4.32$ & $-4.32$ & $-4.26$ & $-4.39$ & $-10.14$ \\
\bottomrule
\end{tabular}
\end{center}

Three observations. \emph{(i) Blind exhibits a long predictive horizon.} Linear $R^2$ from $\mathbf{h}_t$ to $(x, y)_{t+k}$ remains $\geq 0.90$ from $k=0$ to $k=20$ (only $0.04$-point drop), and $0.79$ at $k=50$. That is, the same linear projection that decodes \emph{current} position from $\mathbf{h}_t$ at $R^2 = 0.94$ also decodes positions $20$ steps in the future at $R^2 = 0.90$. This is a strong form of the predictive-map signature predicted by SR theory: $\mathbf{h}_t$ encodes future-state occupancy, not just current state. \emph{(ii) Coarse follows the same pattern at lower magnitude} (linear $R^2 \in [0.50, 0.57]$ across $k=0$--$20$, dropping to $+0.37$ at $k=50$). \emph{(iii) Rich-encoder conditions have no clear predictive horizon.} Foveated stays in $[0.06, 0.19]$ across $k=0$--$50$ with no monotonic decay --- the small positive values reflect probe-level noise rather than a sustained future-state code. Foveated-logpolar starts at $+0.17$ but turns negative by $k=20$. Uniform is already strongly negative at $k=0$ ($R^2 = -4.31$) and grows more negative. Rich-encoder $\mathbf{h}_t$ encodes neither current nor future position linearly --- consistent with our LOSO scene-invariance reading that rich-encoder position codes are scene-conditional and only non-linearly recoverable, but adds the temporal axis: even with non-linear flexibility, future positions are not encoded. The signature is asymmetric: bottleneck conditions encode $\mathbf{h}_t$ as a smoothly-varying integrated GPS code that linearly predicts upcoming positions; rich-encoder conditions encode $\mathbf{h}_t$ reactively to the current visual scene without temporal extrapolation.

\noindent (See Figure~\ref{fig:predictive_horizon} for the predictive-horizon panel referenced from §\ref{sec:h1}.)

\subsection{Mutual-information capacity accounting (MINE)}
\label{app:mine}

To anchor the information-allocation finding in §\ref{sec:h1} on a nat-scale rather than the bounded, model-dependent $R^2$ scale of probes, we estimate $I(\mathbf{h}_2; \text{pos})$ per condition using the Mutual Information Neural Estimator~\citep{belghazi2018mine}. MINE exploits the Donsker--Varadhan dual representation of the KL divergence:
\[
I(X; Z) \geq \sup_T \, \mathbb{E}_{P_{XZ}}[T(x, z)] - \log \mathbb{E}_{P_X \otimes P_Z}[\exp T(x, z)],
\]
with $T_{\theta}: \mathcal{X} \times \mathcal{Z} \to \mathbb{R}$ a neural network optimised by gradient ascent on the right-hand side. Joint samples $(x_i, z_i)$ come from paired $(\mathbf{h}_2, \text{pos})$ along episodes; marginal samples are obtained by within-batch shuffling of $z$. We use a 3-layer MLP with $256$ hidden units, ELU activations, batch size $512$, learning rate $10^{-4}$, $4{,}000$ training steps per condition, and the bias-corrected gradient (eq.~12 of \citealt{belghazi2018mine}) to mitigate the SGD bias of the log-denominator estimate.

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/fig_mine_capacity.pdf}
\caption{\textbf{MINE capacity accounting.} \emph{(a)} MINE training curves: $I_\theta(\mathbf{h}_2; \text{pos})$ in nats vs.\ optimisation step, per condition. All curves converge by $\sim 3{,}000$ steps. \emph{(b)} Final $I(\mathbf{h}_2; \text{pos})$ in bits per condition (mean $\pm$ std across $3$ MINE seeds): Blind $6.01 \pm 0.06$, Coarse $4.55 \pm 0.03$, Foveated $4.30 \pm 0.04$, Foveated-logpolar $4.66 \pm 0.04$, Uniform $4.47 \pm 0.06$. Total mutual information decreases with encoder bandwidth, but the decrease is gentle ($\approx 1.3\times$ blind/uniform) compared to the linear-probe $R^2$ collapse (effectively factor-of-zero for uniform). The within-sighted ordering (foveated $<$ uniform $<$ coarse $<$ foveated-logpolar) is reproducible across MINE seeds (per-cond std $\leq 0.06$ bits, much smaller than the within-sighted gap $\approx 0.36$); the dominant signal is the bottleneck-vs-rich-encoder gap (Blind $6.01$ vs sighted mean $4.49$). The interpretation: rich-encoder conditions retain substantial position information in $\mathbf{h}_2$, but it is encoded non-linearly and partly offloaded to the encoder route, paralleling the LOSO scene-invariance result (Figure~\ref{fig:loso_cv}).}
\label{fig:mine_capacity}
\end{figure}

\subsection{Eigenspectrum power-law per condition}
\label{app:eigenspectrum}

We adapt the eigenspectrum analysis of \citet{stringer2019high} to the LSTM hidden state, fitting a power law $\lambda_n \propto n^{-\alpha}$ to the eigenvalues of $\mathbf{h}_2$ per condition (PCs $n \in [5, 200]$ to skip finite-sample bias and machine-precision tail). All five conditions show a tight power-law eigenspectrum (Figure~\ref{fig:eigenspectrum}, $R^2 \geq 0.99$). The fitted exponent shows a sharp bottleneck-vs-rich-encoder split:

\begin{center}
\begin{tabular}{lccc}
\toprule
Condition & $\alpha$ & $R^2$ & Var. top 10 PCs \\
\midrule
Blind             & $3.85 \pm 0.03$ & $0.99$ & $95\%$ \\
Coarse            & $1.80 \pm 0.01$ & $1.00$ & $82\%$ \\
Foveated          & $1.72 \pm 0.01$ & $1.00$ & $76\%$ \\
Foveated-logpolar & $1.80 \pm 0.01$ & $1.00$ & $95\%$ \\
Uniform           & $1.90 \pm 0.01$ & $1.00$ & $91\%$ \\
\bottomrule
\end{tabular}
\end{center}

Two observations. (i) Blind sits well above the critical exponent $1 + 2/d_{\rm task} \approx 1.67$ that demarcates differentiable from fractal manifolds for a $d_{\rm task} = 3$ stimulus (current position + heading), and far above Stringer's V1 value ($\alpha \approx 1.04$). With $\alpha = 3.85$, blind's eigenspectrum decays sharply: variance is concentrated in the top few PCs (top-$10$ already capture $95\%$). We do not claim that Stringer's specific theoretical bound ($\alpha = 1 + 2/d$, derived for smooth tuning curves over a $d$-dim stimulus manifold) directly applies to our recurrent setting; the observation is descriptive. (ii) The four sighted conditions cluster tightly around $\alpha \approx 1.7$--$1.9$, all near the $1 + 2/d_{\rm task} \approx 1.67$ critical exponent --- the smoothness/efficiency border. Blind is the outlier above, in a few-dim concentrated regime, while the four sighted conditions distribute variance across more PCs. The split bottleneck-vs-rich-encoder reading: blind's heavy reliance on the integrated GPS code crystallises into a low-dimensional eigenspectrum, while sighted conditions (including coarse, whose $1{\times}1$ encoder still feeds a ResNet-18 stack with diverse channels) distribute hidden-state variance across more directions to support encoder-route information.

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/fig_eigenspectrum.pdf}
\caption{\textbf{Eigenspectrum power-law per condition.} \emph{(a)} Eigenvalue $\lambda_n$ vs.\ PC index $n$ for $\mathbf{h}_2$, log--log; reference power laws $\alpha = 1$ (Stringer V1) and $\alpha = 1 + 2/3$ (theoretical critical exponent for $d_{\rm task} = 3$) shown for comparison. \emph{(b)} Fitted power-law exponent $\alpha$ per condition (linear fit on log-$\lambda_n$ vs.\ log-$n$, PCs $5$--$200$); error bars are the std of the slope estimate. The monotonic ordering is consistent with the capacity-allocation principle: rich-encoder conditions distribute variance across more PCs and obtain lower $\alpha$ than bottleneck conditions.}
\label{fig:eigenspectrum}
\end{figure}

\subsection{Persistent-memory failure-mode catalog}
\label{app:shortcut-catalog}

The main-text Figure~\ref{fig:shortcut_paired_traj} shows one canonical paired-episode case per condition; the full $4{\times}4$ catalog below (Figure~\ref{fig:shortcut_catalog}) shows four cases per condition spanning the per-condition margin range (most-tries-new $\to$ most-locks-onto-old).  All cases share the selection criterion: reset SPL $> 0.5$, persistent SPL $< 0.2$, persistent steps $\geq 30$, $|\Delta y_\text{goal}| < 0.5$\,m (same-floor old vs.\ new goal). Within each row, panels are ordered by margin (column 1 = most-negative = closer to NEW goal direction; column 4 = most-positive = closer to OLD goal direction).

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/appfig_shortcut_catalog.pdf}
\caption{Persistent-memory failure-mode catalog: 4 cases per condition spanning the per-condition margin distribution. Each panel: reset trajectory (solid, condition colour) reaches the new goal; persistent trajectory (dashed) demonstrates the failure. The cross-condition pattern: bottleneck conditions (Blind, Coarse) cluster at ``tries-new'' (most cases attempt new goal direction but fail to reach); Uniform clusters at ``locks-onto-old'' (most cases stay near previous-episode goal); Foveated is mixed with no dominant mode. Single seed per condition (cogsci modelling convention; see §\ref{sec:limitations}).}
\label{fig:shortcut_catalog}
\end{figure}

\begin{figure}[!t]
\centering
\begin{minipage}{0.48\linewidth}\centering
\includegraphics[width=\linewidth]{fig/appfig13_layerwise_decay.pdf}\\
{\footnotesize (a) Per-LSTM-layer GPS / compass probe $R^2$ across all three layers, all four conditions (single train/test fit; the 5-fold CV mean reported in Table~\ref{tab:summary} matches qualitatively but with different magnitudes for rich-encoder conditions). Compass tells the same story as GPS at smaller magnitude.}
\end{minipage}\hfill
\begin{minipage}{0.48\linewidth}\centering
\includegraphics[width=\linewidth]{fig/appfig13_mp3d_generalization.pdf}\\
{\footnotesize (b) Companion to Figure~\ref{fig:h1_mega}a (which uses 5-fold CV mean): single train/test fit on the same checkpoints; the qualitative bottleneck-vs-rich-encoder partition is the same.}
\end{minipage}
\caption{Per-layer and MP3D companion figures (referenced from §\ref{sec:h1}).}
\label{fig:supp_layerwise_mp3d}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.85\linewidth]{fig/appfig_wjf_excursion.pdf}
\caption{Excursion-forgetting per-segment scale-invariant error (referenced from §\ref{sec:boundaries}). One Ridge probe trained on full-episode pooled hidden states (episode-level 5-fold CV); the same probe tested on each segment of the held-out fold. Lower MAE/spread $=$ more accurate per-segment position decoding; values $\geq 1$ are at-or-worse than predict-mean baseline. All five conditions show recovery $>$ warmup error (forgetting); blind's rise ($+0.381$) is the largest, roughly $1.8$--$2.8\times$ the sighted conditions ($+0.135$ foveated-logpolar, $+0.167$ uniform, $+0.198$ coarse, $+0.210$ foveated). The pattern is consistent with blind carrying the most action-stream-dependent integrated GPS code and the sighted conditions partly re-anchoring on visual landmarks during recovery. Single seed.}
\label{fig:wjf_excursion}
\end{figure}


\section{Foveation experiments: status and design}
\label{app:foveation-status}

This appendix details the four controlled foveation experiments referenced from the main paper. All four are submitted.

\begin{table}[!t]
\centering
\small
\caption{Foveation experiment status (submission time).}
\label{tab:fov-status}
\footnotesize\begin{tabular}{@{}p{0.32\linewidth}p{0.42\linewidth}p{0.20\linewidth}@{}}
\toprule
Experiment & Tests & Status \\
\midrule
F1--F4 strength sweep ($\sigma_{\max} \in \{2,4,12,20\}$) & encoder--memory race as continuous lever & future work \\
F3 log-polar foveation                                    & spatial-sampling vs.\ blur bottleneck    & in training (RCP probe-4, unified hp) \\
Encoder feature-map probe                                 & where position information is held       & \textbf{landed; see §\ref{sec:discussion}} \\
\bottomrule
\end{tabular}
\end{table}

\emph{Foveation strength sweep (F1--F4) --- planned for follow-up, not in this submission.} The protocol would train four additional foveated agents at $\sigma_{\max} \in \{2, 4, 12, 20\}$ alongside the existing $\sigma_{\max} = 8$, holding gaze location, encoder stack, training budget, and training pool fixed. Expected signature: at low $\sigma_{\max}$ the foveated agent should look like uniform; at high $\sigma_{\max}$ it should approach coarse. The $R^2$ vs.\ $\sigma_{\max}$ curve would directly test the encoder--memory race as a continuous lever rather than a binary one.

\emph{Spatial sampling vs.\ blur (F3 log-polar).} Gaussian blur removes high-frequency content but preserves spatial layout: a $\sigma_{\max}=8$ foveated image still feeds the encoder $256{\times}256$ pixels, so the ResNet-18 stack still produces an $4{\times}4$ spatial feature map. A log-polar foveation~\citep{strasburger2011peripheral,rosenholtz2016peripheral} resamples with a non-uniform retinal grid (e.g.\ $64{\times}64$): the encoder spatial output drops to $\sim 2{\times}2$, much closer to coarse's $1{\times}1$ than to uniform's $4{\times}4$. This isolates the variable our sharpened H1 mechanism identifies as the trigger: \emph{spatial-feature variety in the encoder output}. The preregistered prediction (written before result was available) was that log-polar should produce LSTM GPS $R^2 \geq 0.3$, between coarse's $+0.58$ and uniform's $\approx 0$, with corresponding behavioural shifts; if log-polar matched uniform, the encoder-spatial-output-dimensionality mechanism would be wrong. The result (next paragraph) falsifies that simple reading.

\emph{Why disclose this rather than wait.} Each experiment has a falsifiable signature; we disclose the designs openly so (i)~the submission is honest about what foveation-specific evidence already exists vs.\ what is left for follow-up, and (ii)~the reader can identify which incoming result would change the paper's interpretation.


\section{Encoder-capacity scaling: log-polar foveation control}
\label{app:scaling}

A direct test of the H1 mechanism (encoder spatial-feature variety per step drives top-layer LSTM spatial encoding) is to vary that variable continuously across our existing 4-condition spectrum. The most diagnostic single point is the \emph{log-polar foveation} control: log-polar resampling onto a $64{\times}64$ retinal grid produces an encoder spatial output of $\sim 2{\times}2$, intermediate between coarse's $1{\times}1$ and uniform's $4{\times}4$ (verified by direct forward pass through the trained encoder). Only the visual front-end differs from the foveated condition; LSTM, sensors, and training pipeline are identical. \textbf{Preregistered prediction:} if the H1 mechanism is the simple ``encoder spatial-output dimensionality'' reading, log-polar should produce LSTM GPS $R^2$ in the bottleneck regime (i.e.\ closer to coarse's $+0.58$ than to uniform's $\approx 0$); if log-polar matches uniform on H1 instead ($R^2 \approx 0$), the encoder-spatial-output mechanism story needs reframing toward channel information, encoder spatial-feature \emph{variety} per step, or another axis. \textbf{Result (converged 250M frames).} The log-polar agent at convergence (ckpt-49, $\sim 245$M frames) yields top-layer GPS $R^2 = -0.029 \pm 0.759$ (5-fold CV, $n=500$ episodes) --- \emph{not} in the bottleneck regime, but indistinguishable from foveated ($R^2 = +0.178 \pm 0.659$) and clearly far from coarse ($R^2 = +0.582 \pm 0.099$). The simple ``encoder spatial-output dimensionality'' framing is therefore \emph{not} sufficient to explain the H1 phenomenon: log-polar's $2{\times}2$ output does not place it in the bottleneck regime. We read the result as supporting the alternative ``encoder spatial-feature \emph{variety} per step'' framing: log-polar's radial sampling preserves rich gaze-centred feature variety per timestep even at low spatial output, so the policy can still source position from the visual route and the LSTM does not commit capacity to the integrated code. Substitution-dynamics analysis (Figure~\ref{fig:magnitude}b) corroborates this reading: log-polar starts at $R^2 = +0.92$ at $\sim 50$M frames (matching all 4 main conditions' early high $R^2$), then decays along the rich-encoder trajectory rather than plateauing in the bottleneck range. A finer multi-point input-resolution sweep ($K \in \{32, 64, 96, 128, 192\}$) is reported as future work; its training cost (multiple from-scratch DD-PPO runs at $\sim 250$M frames each) was outside this submission's compute window.


\section{Additional representational and behavioural diagnostics}
\label{app:extra-diagnostics}

This appendix collects companion figures that extend results reported numerically in the main text. They are not load-bearing for any main-text claim and may be skipped on first reading.

\subsection{Magnitude-axis: panel-level detail and predictive-horizon panel}
\label{app:magnitude-detail}

\noindent The §\ref{sec:h1} consolidated 3-panel figure (Figure~\ref{fig:magnitude}) summarises the magnitude-axis story; the figures below preserve the panel-level detail referenced from that section.

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/fig2_h1_mega.pdf}
\caption{\textbf{Magnitude-axis evidence in detail (referenced from §\ref{sec:h1} and Table~\ref{tab:summary}).} \emph{(a)}~Top-layer GPS $R^2$ on Gibson and held-out MP3D (5-fold CV, deterministic rollouts, $300$ ep/cond; lighter bars = MP3D; values outside y-range labelled). \emph{(b)}~Top-layer GPS $R^2$ binned by step-in-episode ($7$ log-spaced bins in $[10, 1600+]$, right-most bin pools $\geq 1024$); rich-encoder conditions hold $R^2 \in [0.6, 0.8]$ in the typical mid-episode window then collapse in the long tail. \emph{(c)}~GPS $R^2$ along Encoder $\to$ L0 $\to$ L1 $\to$ L2 (this panel is condensed into Figure~\ref{fig:magnitude}c; the version here uses single train/test fits rather than 5-fold CV mean). Shaded band at L2: 2-layer MLP probe recovery range ($R^2 \in [0.51, 0.73]$).}
\label{fig:h1_mega}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/fig3_substitution_dynamics.pdf}
\caption{\textbf{Cross-training substitution dynamics: GPS and compass (referenced from §\ref{sec:h1}).} Probe $R^2$ across $4$--$5$ training checkpoints per condition, truncated at $250$M frames. \emph{(a)} Top-layer GPS $R^2$ (this panel is the source for Figure~\ref{fig:magnitude}b). \emph{(b)} Top-layer compass $R^2$ tells the same story at smaller magnitude --- the substitution applies to spatial codes broadly, not just position. ($500$ ep/probe, 5-fold CV, single seed; hollow dotted = partial-data ckpts; negative-$R^2$ outliers clipped at $-2.0$.)}
\label{fig:substitution_dynamics}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.7\linewidth]{fig/fig_predictive_horizon.pdf}
\caption{\textbf{SR-style predictive horizon (referenced from §\ref{sec:h1} and §\ref{app:predictive_horizon}).} Linear $R^2$ predicting future position $(x, y)_{t+k}$ from $\mathbf{h}_t$ vs.\ horizon $k$ (Stachenfeld 2017 SR-style cognitive-map signature). \textsc{Blind} retains $R^2 \geq 0.90$ to $k{=}20$ and $+0.79$ at $k{=}50$; \textsc{coarse} a moderate $R^2 \in [0.50, 0.57]$ to $k{=}20$ ($+0.37$ at $k{=}50$); rich-encoder conditions show no predictive horizon (foveated stays in $[0.06, 0.19]$ flat, foveated-logpolar turns negative by $k{=}20$, uniform is strongly negative throughout). Blind's $\mathbf{h}_t$ encodes future-state occupancy --- the canonical cognitive-map signature predicted by SR theory.}
\label{fig:predictive_horizon}
\end{figure}

\subsection{Other diagnostics}
\label{app:other-diagnostics}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/fig_place_cells.pdf}
\caption{\textbf{Skaggs spatial information per LSTM unit (referenced from §\ref{sec:h1}).} \emph{(a)} Distribution of per-unit Skaggs bits (averaged across 15 top scenes per condition); bottleneck and rich-encoder conditions overlap substantially (means $\approx 1.16$--$1.32$ bits). \emph{(b)} Number of place units per scene exceeding the $99$th percentile of a $100$-shuffle null; foveated has the largest count ($\approx 237$/scene), blind/coarse/uniform $\approx 116$--$174$. \emph{(c)} Distribution summary; cross-condition differences sit within the within-condition spread. The canonical Skaggs metric on rectified activations does not by itself discriminate bottleneck from rich-encoder regimes; the per-unit-regression analysis in main-text §\ref{sec:h1} (trajectory-phase vs.\ raw-position $R^2$) is the more diagnostic reading.}
\label{fig:place_cells}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=0.95\linewidth]{fig/fig_subspace_evolution.pdf}
\caption{\textbf{Subspace evolution over training (referenced from §\ref{sec:format-spatial}).} \emph{(a)}~Mean principal angle between condition-pair top-$K$ subspaces, plotted across five training stages ($\sim 50$M to $\sim 250$M frames; sighted at ckpt $10$/$20$/$30$/$40$/$49$, blind at $10$/$15$/$20$/$25$). Angles are already $[82°, 89°]$ at the earliest probed stage and stay near-orthogonal throughout. \emph{(b)}~Position-encoding direction cosines are essentially zero throughout (range $[-0.05, +0.14]$), indicating direction divergence is committed before the earliest probed checkpoint. Single seed; 4--5 ckpts per condition. Companion to main-text Figure~\ref{fig:subspace_divergence}.}
\label{fig:subspace_evolution}
\end{figure}

\begin{figure}[!t]
\centering
\includegraphics[width=\linewidth]{fig/fig5_shortcut_canonical.pdf}
\caption{\textbf{Persistent-memory failure modes (referenced from §\ref{sec:dissociation}): canonical paired-episode case per condition.} Extended catalog: Figure~\ref{fig:shortcut_catalog}. Aggregate per-condition margin $=$ avg dist(persistent end $\to$ old goal) $-$ avg dist(persistent end $\to$ new goal) over same-floor failures (reset SPL $>0.5$, persistent SPL $<0.2$, persistent steps $\geq 30$, $|\Delta y_\text{goal}| < 0.5$m): Blind $-0.38$m ($n{=}27$), Coarse $-0.57$m ($n{=}35$), Foveated $-0.59$m ($n{=}16$), \textbf{Uniform $+1.83$m ($n{=}46$, closer to OLD goal)}. Single seed.}
\label{fig:shortcut_paired_traj}
\end{figure}


\end{document}
